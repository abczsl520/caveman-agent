# Core dependencies (always installed)
typer>=0.12
rich>=13
httpx>=0.27
pyyaml>=6
pydantic>=2.7
aiofiles>=23

# Common provider deps (install with: pip install caveman[providers])
# anthropic>=0.25
# openai>=1.30

# For full install: pip install caveman[all]
