# Caveman API Reference

> Auto-generated by `scripts/generate_api_reference.py`. Do not edit by hand.

## `caveman.__init__`

Caveman — an agent that learns, executes, and evolves.

## `caveman.__main__`

Caveman — The Self-Evolving AI Agent Framework.

## `caveman.acp.__init__`

ACP — Agent Client Protocol for Caveman.

## `caveman.acp.client`

ACP Client — connect to another ACP-compatible agent.

**Classes:** `ACPClient`

## `caveman.acp.copilot_client`

OpenAI-compatible shim that forwards requests to `copilot --acp`. Full JSONRPC protocol over stdio with subprocess lifecycle management.

**Classes:** `CopilotMessage`, `CopilotSession`, `CopilotACPClient`

## `caveman.acp.copilot_protocol`

Copilot ACP protocol helpers — message formatting, tool call extraction. Extracted from copilot_client.py to keep modules under 450 lines.

## `caveman.acp.events`

ACP Event Callbacks — bridge AgentLoop events to ACP protocol updates. Translates Caveman's internal events (tool calls, thinking, messages) into ACP-compatible session updates for connected editors/clients. Learned from

**Classes:** `ACPEvent`, `ACPEventEmitter`

**Functions:** `get_tool_kind()`, `make_tool_call_id()`, `build_tool_title()`

## `caveman.acp.server`

ACP Server v2 — full ACP protocol with session management. Exposes Caveman as an ACP-compatible agent with: - Session lifecycle (create/load/resume/fork/list) - SSE streaming for real-time events - Slash commands (/model

**Classes:** `ACPTask`, `ACPServer`

## `caveman.acp.session`

ACP Session Manager — persistent, thread-safe session lifecycle. Maps ACP sessions to Caveman AgentLoop instances. Sessions survive process restarts via SQLite persistence. Learned from: Hermes acp_adapter/session.py (47

**Classes:** `ACPSessionState`, `ACPSessionManager`

## `caveman.agent.__init__`

Agent core — loop, context, phases, tool execution, prompt assembly, and workspace.

## `caveman.agent.auxiliary`

Auxiliary LLM client — cheap model for side tasks. Ported from Hermes agent/auxiliary_client.py (2613 lines → ~180 lines). Uses a cheap/fast model for auxiliary tasks like: - Session title generation - Quick classificati

**Classes:** `AuxiliaryClient`

**Functions:** `generate_title()`, `classify_intent()`, `extract_tags()`

## `caveman.agent.auxiliary_client`

Auxiliary Client — secondary LLM client for summarization and analysis. Provides a lightweight LLM client for auxiliary tasks (summarization, classification, analysis) that doesn't use the primary model's context. Extrac

**Classes:** `AuxiliaryConfig`

**Functions:** `call_llm()`, `summarize()`, `classify()`, `translate()`

## `caveman.agent.behavior_rules`

Behavior rules — single source of truth for agent output conventions. All closing formats, output styles, and behavioral constants live here. Other modules import via get_rule() instead of hardcoding values.

**Functions:** `get_rule()`

## `caveman.agent.bg_tasks`

Background task helpers for AgentLoop. Extracted from loop.py to keep it under 400 lines (NFR-502). All methods here are called by AgentLoop but don't need direct access to the main run() pipeline state.

**Classes:** `BackgroundTaskMixin`

## `caveman.agent.checkpoint`

Checkpoint system — save and restore agent state.

**Classes:** `Checkpoint`, `CheckpointManager`

## `caveman.agent.context`

Context manager — manages agent context window and compression.

**Classes:** `Message`, `AgentContext`

## `caveman.agent.context_compressor`

Context Compressor — automatic conversation context compression. Compresses long conversations by summarizing middle turns while protecting head (system) and tail (recent) messages. Supports iterative summary updates acr

**Classes:** `CompactionResult`, `ContextCompressor`

**Functions:** `get_context_length()`, `estimate_tokens_rough()`

## `caveman.agent.context_engine`

Context Engine — pluggable context management interface. Defines the ContextEngine protocol and provides a default implementation that combines compaction, pruning, and assembly. Extracted from OpenClaw src/context-engin

**Classes:** `AssembleResult`, `CompactResult`, `IngestResult`, `BootstrapResult`, `EngineInfo`, `ContextEngine`, `DefaultContextEngine`

**Functions:** `register_engine()`, `get_engine()`, `list_engines()`

## `caveman.agent.context_references`

Context References — compat shim. Thin wrapper around context_refs.py for backward compatibility. New code should use caveman.agent.context_refs directly.

**Classes:** `Reference`

**Functions:** `detect_references()`, `resolve_file_reference()`, `resolve_references()`, `build_context_from_references()`

## `caveman.agent.context_refs`

Context references — @file, @url, @diff inline expansion. Inspired by Hermes context_references.py (MIT, Nous Research). Parses @file:path, @url:url, @diff references in user messages and expands them into context blocks

**Classes:** `ContextRef`, `ExpansionResult`

**Functions:** `parse_refs()`, `expand_refs()`

## `caveman.agent.conversation_lifecycle`

Conversation lifecycle awareness — dynamic format rules based on dialog state. Instead of static format rules that never change, this module provides context-aware formatting guidance that adapts to: - Conversation compl

**Classes:** `ConversationComplexity`, `ConversationPhase`, `ConversationState`

**Functions:** `get_phase_rules()`, `get_section_markers()`

## `caveman.agent.display`

Display callbacks — UI output functions used by the agent loop. This module exists to break the loop→tui→factory→loop circular dependency. The agent loop needs display functions, but shouldn't depend on the CLI layer. In

**Functions:** `show_tool_call()`, `show_tool_result()`, `show_memory_nudge()`, `show_skill_nudge()`, `show_error()`, `show_thinking()`

## `caveman.agent.factory`

Factory — create AgentLoop from config file.

**Functions:** `create_loop()`

## `caveman.agent.iteration_budget`

Iteration budget — thread-safe iteration counter with refund support. Ported from Hermes IterationBudget (MIT, Nous Research), adapted for Caveman. Each agent (parent or sub-agent) gets its own budget. - Parent: capped a

**Classes:** `IterationBudget`

## `caveman.agent.loop`

Core agent loop v3 — thin orchestrator over decomposed phases.

**Classes:** `AgentLoop`

## `caveman.agent.loop_engines`

Loop engine helpers — post-task engine orchestration extracted from loop.py.

**Functions:** `build_user_content()`, `prepare_multi_turn()`, `post_task_engines()`, `record_turn_metrics()`, `check_termination()`, `update_shield()`, `run_preemptive_compaction()`

## `caveman.agent.loop_state`

LoopState — single source of truth for serializable session state. Adding a field here automatically propagates to snapshot/restore/reset.

**Classes:** `LoopState`

## `caveman.agent.memory_manager`

MemoryManager — orchestrates built-in + at most ONE external provider. .. deprecated:: 0.3.0 This module is dead code. The production MemoryManager lives in caveman.memory.manager. Will be removed in 0.4.0. Full port fro

**Classes:** `MemoryManager`

## `caveman.agent.memory_provider`

Abstract base class for pluggable memory providers. .. deprecated:: 0.3.0 This module is dead code. The production MemoryManager lives in caveman.memory.manager. Will be removed in 0.4.0. Ported from Hermes agent/memory_

**Classes:** `MemoryProvider`, `BuiltinMemoryProvider`

## `caveman.agent.metrics`

Agent performance metrics — track timings, counters, and percentiles.

**Classes:** `AgentMetrics`

## `caveman.agent.output_validator`

Output format validator — suppresses premature terminal markers. The canonical ✅---本轮已完成---✅ marker is retained only as a legacy token for recognition/stripping. It must not be synthesized or preserved by default: comple

**Functions:** `final_text_looks_truncated()`, `final_sentence_is_question()`, `should_use_closing_marker()`, `strip_closing_markers()`, `enforce_closing_format()`

## `caveman.agent.phased_coordinator`

Coordinator — orchestrates 4 phases: Explore → Plan → Execute → Verify.

**Classes:** `Phase`, `PhasedCoordinator`

## `caveman.agent.phases`

Agent phases — decomposed pipeline stages for the agent loop. Extracted from loop.py to keep it under 400 lines. Each phase is a standalone async function that takes explicit dependencies.

**Functions:** `phase_prepare()`, `phase_compress()`, `phase_llm_call()`, `record_assistant_turn()`, `phase_finalize()`

## `caveman.agent.prompt`

System prompt builder — layered assembly with token budgets. Upgraded with insights from Hermes prompt_builder.py (MIT, Nous Research). Each layer has a token budget; layers are assembled in priority order.

**Classes:** `PromptSection`, `PromptLayer`, `PromptBuildResult`, `PromptBuilder`

**Functions:** `scan_content()`, `build_system_prompt()`

## `caveman.agent.prompt_builder`

Prompt Builder — system prompt construction with context files. Builds the system prompt from SOUL.md, AGENTS.md, project context, skills manifest, and environment hints. Extracted from Hermes agent/prompt_builder.py (10

**Classes:** `SkillEntry`, `PromptConfig`

**Functions:** `find_git_root()`, `strip_yaml_frontmatter()`, `truncate_content()`, `scan_context_content()`, `load_context_file()`, `load_soul_md()`, `build_environment_hints()`, `build_skills_manifest()`, `build_skills_prompt()`, `build_system_prompt()`

## `caveman.agent.prompt_caching`

Prompt Caching — cache system prompts to reduce token usage. Caches the system prompt hash and marks it for provider-level caching (Anthropic cache_control, OpenAI cached tokens).

**Classes:** `CacheEntry`, `PromptCache`

## `caveman.agent.prompt_contract`

Prompt Contract — defines layer boundaries to prevent conflicting instructions. Each layer has explicit allowed/forbidden topics. The PromptBuilder validates content against these contracts at build time, catching contra

**Classes:** `PromptTopic`, `LayerContract`, `ContractViolation`

**Functions:** `detect_topics()`, `validate_layer()`, `validate_all_layers()`

## `caveman.agent.rate_limit_tracker`

Rate Limit Tracker — API rate limit tracking and backoff. Tracks API rate limits per provider/model and implements intelligent backoff. Extracted from Hermes agent/rate_limit_tracker.py.

**Classes:** `RateLimitState`, `RateLimitTracker`

## `caveman.agent.response_style`

Surface-aware response style rules. Injected as a system prompt layer so the agent automatically adapts its formatting to the output surface (Discord, Telegram, CLI, etc.). NOTE: ✅ and ❌ are deliberately excluded from se

**Functions:** `get_response_style()`, `get_format_reminder()`

## `caveman.agent.session_db`

SQLite session store — replaces JSON file storage. Drop-in replacement for SessionStore with: - Atomic writes (no data loss on concurrent access) - Queryable history (SQL instead of full-file reads) - System prompt persi

**Classes:** `SessionDB`

## `caveman.agent.session_hooks`

Session lifecycle hooks — on_session_end knowledge extraction. Inspired by Hermes MemoryProvider on_session_end hook. Extracts final knowledge from a completed session before cleanup. Integrates with Wiki Compiler for kn

**Functions:** `on_session_end()`, `on_delegation_complete()`

## `caveman.agent.session_store`

Session persistence — transcript storage and restoration. Learned from OpenClaw's session management: - Full transcript stored on disk (JSON lines) - Compaction summaries preserved alongside raw history - Session metadat

**Classes:** `SessionMeta`, `SessionStore`

## `caveman.agent.smart_model_routing`

Smart Model Routing — cheap vs strong model selection per turn. Routes simple messages to a cheaper model while keeping complex tasks on the primary model. Extracted from Hermes agent/smart_model_routing.py (195 lines).

**Classes:** `RoutingConfig`, `RouteDecision`

**Functions:** `classify_message_complexity()`, `choose_route()`

## `caveman.agent.stream`

Stream primitives — StreamEvent + StreamBuffer. The streaming implementation lives in AgentLoop.run_stream() (loop.py). This module only contains the data types to avoid circular imports.

**Classes:** `StreamEvent`, `StreamBuffer`

## `caveman.agent.subdirectory_hints`

Subdirectory Hints — codebase navigation assistance. Generates intelligent hints about directory structure to help the agent navigate large codebases efficiently without reading every file. Extracted from Hermes agent/su

**Classes:** `DirHint`

**Functions:** `generate_hints()`, `format_hints_for_prompt()`

## `caveman.agent.title_generator`

Auto-generate short session titles from the first user/assistant exchange. Runs after the first response so it never adds latency to the user-facing reply. Uses the auxiliary client (cheapest model) with heuristic fallba

**Functions:** `generate_title()`, `auto_title_session()`

## `caveman.agent.tools_exec`

Tool execution helpers — extracted from loop.py.

**Functions:** `execute_tool()`, `phase_tool_execution()`

## `caveman.agent.workspace`

Workspace file reader — load SOUL.md, USER.md, etc. into prompt layers. Scans workspace directories for persona/context files and injects them into the system prompt with proper layering (SOUL > USER > MEMORY > AGENTS > 

**Classes:** `WorkspaceLoader`

## `caveman.agent.workspace_memory_sync`

Workspace → Memory Store sync — keeps vector DB in sync with workspace files. PRD §8.8.1: MEMORY.md should be "读取 + 导入到 Memory Store". This module makes it automatic: on session start, detect changes in workspace files a

**Classes:** `WorkspaceMemorySync`

## `caveman.aio`

Async wrappers for blocking file I/O operations. Thin wrappers around pathlib.Path and builtins that run in a thread pool via asyncio.to_thread(), preventing event-loop stalls in async code. Usage: from caveman.aio impor

**Functions:** `aio_read_text()`, `aio_write_text()`, `aio_read_bytes()`, `aio_write_bytes()`, `aio_exists()`, `aio_is_file()`, `aio_is_dir()`, `aio_stat()`, `aio_mkdir()`, `aio_glob()`, `aio_iterdir()`, `aio_unlink()`, `aio_rename()`, `aio_rglob()`

## `caveman.audit`

Audit log — structured event export for compliance and debugging. Exports EventBus events + EventStore history to JSONL format. Supports filtering by time range, event type, and source.

**Functions:** `export_audit_log()`, `query_audit_log()`

## `caveman.bridge.__init__`

Bridge layer — UDS, MCP, ACP, WebSocket, CLI transport, and CLI agent protocols.

## `caveman.bridge.acp`

ACP (Agent Communication Protocol) integration. ACP enables Caveman to: 1. Be spawned BY OpenClaw as an ACP agent 2. Spawn OTHER agents (Claude Code, Codex, etc.) as sub-agents 3. Participate in multi-agent orchestration

**Classes:** `ACPEventType`, `ACPSession`, `ACPServer`, `ACPClient`

## `caveman.bridge.cli_agents`

CLI agent runner — delegate coding tasks to external AI agents. Supports Claude Code, Codex, and other CLI-based coding agents via subprocess invocation with timeout and output capture.

**Classes:** `CLIAgentError`, `CLIAgentResult`, `CLIAgentRunner`

## `caveman.bridge.cli_transport`

CLI transport — Layer 1 OpenClaw bridge via subprocess. Pros: handles all auth, always works, no WebSocket complexity Cons: per-command overhead (~200ms), no streaming, no events

**Classes:** `CLITransport`

## `caveman.bridge.hermes_bridge`

Hermes bridge — interop with Hermes Agent for skill/trajectory exchange. Hermes Agent API (v0.7+): - POST /api/delegate: Delegate task to Hermes - GET /api/skills: List skills - GET /api/skills/{name}: Get skill details 

**Classes:** `HermesBridge`

## `caveman.bridge.openclaw_bridge`

OpenClaw Bridge — connect Caveman to OpenClaw's messaging and tool ecosystem. Architecture (Route C — layered parasitism): Layer 1: CLI subprocess via `openclaw agent` — simple, reliable, handles auth Layer 2: WebSocket 

**Classes:** `OpenClawBridge`

## `caveman.bridge.uds_transport`

Unix Domain Socket (UDS) high-frequency bridge transport. UDS = 0-latency IPC for local Caveman ↔ OpenClaw/Hermes communication. HTTP bridge works but adds ~5ms overhead per call. UDS eliminates that. Protocol: JSON-RPC 

**Classes:** `UDSClient`, `UDSServer`

## `caveman.bridge.ws_transport`

WebSocket transport — direct connection to OpenClaw Gateway. Layer 2 transport: bypasses CLI subprocess overhead (~3s → <100ms). Speaks the OpenClaw Gateway protocol: JSON frames over WebSocket. Protocol: Client → Gatewa

**Classes:** `WSTransportError`, `WSTransport`

## `caveman.cli.__init__`

CLI interface — commands, TUI, diagnostics, and import.

## `caveman.cli.acp_cli`

CLI handlers for ACP commands.

**Functions:** `acp_serve()`, `acp_send()`

## `caveman.cli.adapt`

CLI: adapt-workspace command.

**Functions:** `adapt_workspace()`

## `caveman.cli.audit`

Caveman Audit — static code quality checks without LLM. Usage: caveman audit [--fix] Checks: 1. open() without encoding 2. UUID truncation in memory IDs 3. bare except / swallowed exceptions 4. files over 400 lines (NFR-

**Functions:** `check_encoding()`, `check_uuid_truncation()`, `check_swallowed_exceptions()`, `check_file_size()`, `run_audit()`

## `caveman.cli.backup`

Backup Manager — configuration and data backup. Provides backup/restore for caveman configuration, sessions, and memory. Extracted from Hermes hermes_cli/backup.py.

**Classes:** `BackupManifest`, `BackupManager`

## `caveman.cli.bench`

Caveman Bench — memory system performance benchmarks. Usage: caveman bench [--rounds N] Measures: 1. Memory store latency 2. Memory recall latency 3. Embedding generation latency 4. Shield update latency

**Functions:** `run_bench()`, `run_bench_sync()`

## `caveman.cli.changelog`

Caveman Changelog — auto-generate from git log.

**Functions:** `generate_changelog()`

## `caveman.cli.code_health`

Code health checker — single source of truth for NFR-502. Reads policy from pyproject.toml [tool.caveman.code-health]. All tests should call `check_code_health()` instead of hardcoding thresholds. Two checks: 1. File siz

**Functions:** `check_code_health()`, `format_report()`

## `caveman.cli.doctor`

Caveman Doctor v2 — flywheel health diagnostics with 6 metrics. `caveman doctor` reports on the health of the learning flywheel: 1. Skill match rate — how often skills are correctly selected 2. Memory recall precision — 

**Classes:** `DoctorReport`

**Functions:** `run_doctor()`

## `caveman.cli.flywheel`

Caveman Flywheel — self-improvement loop. Usage: caveman flywheel [--rounds N] [--target SUBSYSTEM] caveman flywheel --all caveman flywheel --parallel tools memory agent caveman flywheel --stats Runs Caveman against its 

**Classes:** `FlywheelStats`

**Functions:** `run_flywheel()`, `run_flywheel_sync()`, `run_flywheel_parallel()`, `discover_subsystems()`, `flywheel_cli()`

## `caveman.cli.importer`

Memory importer — orchestrator for the modular import system. `caveman import --from openclaw` reads data from external sources and writes them into Caveman's Memory Store. Delegates to caveman.import_.* modules for sour

**Functions:** `detect_sources()`, `import_memories()`

## `caveman.cli.main`

Caveman CLI entry point.

**Functions:** `main_callback()`, `run()`, `skills()`, `version()`, `tools()`, `setup()`, `serve()`, `export()`, `import_cmd()`, `doctor()`, `adapt_workspace_cmd()`, `train()`, `hub()`, `plugins()`

## `caveman.cli.migrate`

Migration CLI helpers.

**Functions:** `run_migrate()`

## `caveman.cli.model_normalize`

Model Normalize — model name normalization and alias resolution. Normalizes model names across different formats and resolves aliases. Extracted from Hermes hermes_cli/model_normalize.py.

**Functions:** `resolve_alias()`, `detect_provider()`, `normalize_model()`, `list_known_models()`

## `caveman.cli.selftest`

Caveman Self-Test — full lifecycle verification. Runs: store → recall → shield → reflect → wiki Verifies the entire memory flywheel works end-to-end.

**Functions:** `run_self_test()`

## `caveman.cli.stats`

Caveman Stats — project statistics at a glance.

**Functions:** `get_stats()`

## `caveman.cli.status`

CLI Status Dashboard — quick overview of Caveman's state.

**Functions:** `status_text()`, `main()`

## `caveman.cli.tui`

Caveman TUI — rich terminal UI components + interactive REPL. Two modes: 1. Components: show_banner(), show_tool_call(), etc. (used by AgentLoop) 2. Interactive REPL: `caveman` with no args → multi-turn conversation

**Functions:** `show_banner()`, `show_status()`, `show_tool_call()`, `show_tool_result()`, `show_memory_nudge()`, `show_skill_nudge()`, `show_error()`, `show_thinking()`, `create_skills_table()`, `interactive_loop()`

## `caveman.cli.utility_commands`

CLI commands: service, obsidian, status, flywheel, audit, bench, etc.

**Functions:** `register_utility_commands()`

## `caveman.cli.wiki_mcp`

CLI handlers for wiki and mcp commands.

**Functions:** `register_wiki_commands()`, `register_mcp_commands()`

## `caveman.commands.__init__`

Caveman slash command system — registry architecture with 50+ commands.

## `caveman.commands.completer`

Auto-completion for slash commands.

**Classes:** `CommandCompleter`

## `caveman.commands.dispatcher`

Unified command dispatcher — routes commands to handlers across surfaces.

**Functions:** `parse_command()`, `dispatch()`

## `caveman.commands.formatter`

Multi-platform output formatting. CLI uses Rich panels/tables; other surfaces get Markdown or plain text.

**Functions:** `format_panel()`, `format_table()`, `format_help()`

## `caveman.commands.gateway_adapter`

Platform adapters for Discord and Telegram command registration.

**Functions:** `discord_slash_commands()`, `telegram_bot_commands()`

## `caveman.commands.handlers.__init__`

Command handler modules.

## `caveman.commands.handlers._helpers`

Shared utilities for command handlers. Eliminates repeated patterns.

**Functions:** `read_json()`, `count_files()`, `list_files()`, `memory_count()`, `memory_search_fts()`, `memory_stats()`, `load_config_safe()`, `memory_recent()`, `memory_top_retrieved()`, `memory_high_trust()`

## `caveman.commands.handlers.caveman`

Caveman-exclusive command handlers.

**Functions:** `handle_flywheel()`, `handle_trajectory()`, `handle_bench()`, `handle_selftest()`, `handle_wiki()`

## `caveman.commands.handlers.config`

Configuration command handlers.

**Functions:** `handle_model()`, `handle_provider()`, `handle_reasoning()`, `handle_fast()`, `handle_verbose()`, `handle_personality()`, `handle_yolo()`, `handle_voice()`, `handle_skin()`, `handle_config()`, `handle_elevated()`, `handle_activation()`

## `caveman.commands.handlers.info`

Info & diagnostics command handlers.

**Functions:** `handle_status()`, `handle_help()`, `handle_commands()`, `handle_usage()`, `handle_insights()`, `handle_doctor()`, `handle_memory()`, `handle_recall()`, `handle_shield()`, `handle_reflect()`, `handle_audit()`, `handle_ratelimit()`

## `caveman.commands.handlers.session`

Session command handlers.

**Functions:** `handle_new()`, `handle_clear()`, `handle_history()`, `handle_retry()`, `handle_undo()`, `handle_title()`, `handle_branch()`, `handle_compress()`, `handle_rollback()`, `handle_stop()`, `handle_approve()`, `handle_deny()`, `handle_background()`, `handle_btw()`, `handle_queue()`

## `caveman.commands.handlers.system`

System command handlers.

**Functions:** `handle_restart()`, `handle_update()`, `handle_profile()`, `handle_sethome()`, `handle_quit()`

## `caveman.commands.handlers.tools`

Tools & skills command handlers.

**Functions:** `handle_tools()`, `handle_skills()`, `handle_engines()`, `handle_cron()`, `handle_reload_mcp()`, `handle_browser()`, `handle_plugins()`, `handle_import()`

## `caveman.commands.registry`

Command registry — central catalog of all slash commands. Provides: - COMMAND_REGISTRY: list of all CommandDef instances - resolve_command(): name/alias → CommandDef lookup - get_by_category(): grouped view - rebuild_loo

**Functions:** `rebuild_lookups()`, `resolve_command()`, `get_by_category()`

## `caveman.commands.types`

Core data types for the slash command system.

**Classes:** `CommandDef`, `CommandContext`

## `caveman.compression.__init__`

Context compression — 3-layer pipeline with smart LLM summarization.

## `caveman.compression.context_engine`

Context Engine — pluggable interface for context management. Ported from Hermes agent/context_engine.py (184 lines). Defines the ABC that all context engines must implement. The built-in SmartCompressor is the default im

**Classes:** `CompressionEngine`

## `caveman.compression.pipeline`

3-layer context compression pipeline. Layer 1 (Micro): Dedup + whitespace normalization — always runs, ~10% reduction Layer 2 (Normal): Tool result truncation + old message pruning — runs when >60% context Layer 3 (Smart

**Classes:** `CompressionStats`, `CompressionPipeline`

## `caveman.compression.preemptive`

Preemptive compaction — proactive context management before overflow. Ported from OpenClaw's preemptive-compaction.ts + tool-result-truncation.ts with Caveman adaptations. Strategy (3-tier, cheapest first): 1. Truncate o

**Classes:** `CompactionRoute`, `PreemptiveResult`

**Functions:** `truncate_tool_result_text()`, `calculate_max_tool_result_chars()`, `count_prunable_images()`, `prune_images()`, `should_preemptively_compact()`, `apply_tool_result_truncation()`, `apply_image_pruning()`

## `caveman.compression.safeguard`

Compaction Safeguard — delay compression during critical phases. Inspired by OpenClaw compaction-safeguard.ts (MIT, Peter Steinberger). Prevents compaction from interrupting critical operations like: - Active tool execut

**Classes:** `SafeguardPhase`, `SafeguardState`, `CompactionSafeguard`

**Functions:** `extract_identifiers()`, `audit_summary()`, `build_compaction_instructions()`

## `caveman.compression.smart`

Smart context compression — LLM-assisted structured summarization. Ported from Hermes ContextCompressor (MIT, Nous Research) with Caveman adaptations and OpenClaw identifier preservation (MIT, Peter Steinberger). Key fea

**Classes:** `SmartCompressor`

## `caveman.compression.utils`

Compression utilities — tool pair sanitization, boundary alignment, estimation. Extracted from smart.py to comply with NFR-502 (max 400 lines per file).

**Functions:** `estimate_tokens()`, `estimate_msg_tokens()`, `sanitize_tool_pairs()`, `align_forward()`, `align_backward()`, `serialize_turns()`, `build_template()`

## `caveman.config.__init__`

Configuration — loading, validation, and defaults.

## `caveman.config.loader`

Config loader — load and validate Caveman configuration.

**Functions:** `invalidate_config_cache()`, `load_config()`

## `caveman.config.validator`

Config validation — fail fast on bad configuration. Instead of silently using wrong config values, validate at startup and give clear error messages. Catches typos, wrong types, missing required fields.

**Functions:** `validate_config()`, `get_config_help()`

## `caveman.config.watcher`

Hot config reload — watch config file for changes and notify listeners. Uses file mtime polling (no external deps like watchdog). Listeners register callbacks that receive the new config dict.

**Classes:** `ConfigWatcher`

## `caveman.coordinator.__init__`

Coordinator — multi-agent orchestration with verification.

## `caveman.coordinator.engine`

Coordinator — multi-agent orchestration engine. The Coordinator manages parallel/sequential agent execution: - Task decomposition: Break complex tasks into sub-tasks - Agent routing: Assign sub-tasks to best-fit agents -

**Classes:** `TaskStatus`, `SubTask`, `ExecutionPlan`, `Coordinator`

## `caveman.coordinator.orchestrator`

Sub-agent Orchestration — spawn, monitor, and learn from sub-agents. Builds on top of Coordinator to provide: - Agent registry with capabilities and cost profiles - Smart routing: match tasks to best-fit agents - Knowled

**Classes:** `AgentProfile`, `AgentRegistry`, `DelegationResult`, `SubAgentOrchestrator`

## `caveman.coordinator.verification`

Verification Agent — anti-rationalization checks for Coordinator tasks. Inspired by Claude Code's 6-excuse blocking pattern: 1. "I already checked" → Verify independently 2. "It looks correct" → Run actual tests 3. "The 

**Classes:** `VerifyResult`, `VerificationReport`, `VerificationAgent`

## `caveman.cron.__init__`

Cron system — scheduled task execution for Caveman. Design (inspired by OpenClaw's 59-file cron system, simplified): - Jobs stored in SQLite (same DB as sessions) - Each job runs in an isolated session - Cron expressions

**Classes:** `JobStatus`, `CronJob`, `CronRun`, `CronStore`, `CronScheduler`

## `caveman.db`

Centralized SQLite connection factory. Every sqlite3.connect() in the codebase should go through this module. Ensures consistent PRAGMA settings (WAL, busy_timeout, foreign_keys) and provides a single place to tune SQLit

**Functions:** `connect()`

## `caveman.engines.__init__`

Caveman OS engines — the 6 kernel engines that power the learning flywheel.

## `caveman.engines.event_chain`

Event chain — wires the inner flywheel: Shield → Nudge → Ripple → Lint → Recall. PRD §5.3: "This is the Agent OS kernel heartbeat. Users don't see it, but without it the outer flywheel can't turn." PRD §4.3 Nudge trigger

**Functions:** `wire_inner_flywheel()`, `unwire_inner_flywheel()`

## `caveman.engines.flags`

Engine feature flags — config-driven + runtime toggle. Controls which engines (shield, nudge, ripple, lint, recall) are active. Config sets defaults; runtime API allows dynamic enable/disable.

**Classes:** `EngineError`, `EngineFlags`

## `caveman.engines.lint`

Lint Engine — knowledge audit and garbage collection. `caveman doctor` runs Lint to detect: - Stale memories (reference deleted files, outdated IPs, old versions) - Contradictions (two memories say opposite things) - Orp

**Classes:** `IssueSeverity`, `IssueType`, `LintIssue`, `LintReport`, `LintEngine`

## `caveman.engines.manager`

Engine Manager — unified lifecycle for all cognitive engines. Replaces the scattered engine initialization in AgentLoop.__init__ and factory.py with a single manager that handles: - Engine creation from config - Dependen

**Classes:** `EngineSet`, `EngineManager`

## `caveman.engines.outcome`

Outcome Signal Engine — score task completion, feed RL Router + memory trust. After each task, analyzes the trajectory to determine success/failure, then propagates that signal to: 1. RL Router — update arm stats for mat

**Classes:** `OutcomeEngine`

## `caveman.engines.project_identity`

Project Identity — persistent project-level context that survives compaction. Unlike SessionEssence (per-session, volatile), ProjectIdentity is: - Per-project, not per-session - Rarely changes (mission, principles are st

**Classes:** `ProjectIdentity`, `ProjectIdentityStore`

**Functions:** `detect_project_from_messages()`

## `caveman.engines.recall`

Recall Engine — restore context from previous sessions. On new session startup, reads the most recent session_essence + relevant memories and injects them into the system prompt via PromptBuilder layers. The Recall Engin

**Classes:** `RecallResult`, `RecallEngine`

## `caveman.engines.reflect`

Reflect Engine — post-task reflection and skill evolution. Inspired by Memento-Skills (arxiv 2603.18743) Read-Execute-Reflect-Write cycle. After task completion, analyzes execution trajectory to: 1. Extract effective pat

**Classes:** `Reflection`, `ReflectEngine`

## `caveman.engines.ripple`

Ripple Engine — knowledge propagation on memory write. When new knowledge is written to Memory Store, Ripple: 1. Searches for related existing entries (semantic + keyword) 2. Detects contradictions (same topic, different

**Classes:** `RippleEffect`, `RippleEngine`

## `caveman.engines.scheduler`

LLM Scheduler — priority-based request queuing for shared LLM access. Problem: Shield, Nudge, SmartCompression, Verification all need LLM calls. Without coordination they race, waste tokens, and can exceed rate limits. D

**Classes:** `Priority`, `LLMRequest`, `CallerStats`, `LLMScheduler`

## `caveman.engines.shield`

Compaction Shield — the agent's "continuous self". Maintains a structured session_essence that survives compaction. After each turn, analyzes conversation to update decisions/progress/stances/key_data/todos. Heuristic ex

**Classes:** `ShieldError`, `SessionEssence`, `CompactionShield`

## `caveman.environments.__init__`

Remote execution environments — SSH, Docker, local. Provides a unified interface for executing commands in different environments. The agent doesn't need to know where code runs — it just calls execute().

**Classes:** `ExecResult`, `Environment`, `LocalEnv`, `SSHConfig`, `SSHEnv`, `DockerEnv`

**Functions:** `create_env()`

## `caveman.errors`

Exception hierarchy — structured errors for the entire framework. One hierarchy, clear categories, machine-parseable. Every exception carries enough context for logging + debugging. Base: CavemanError ├── ConfigError — b

**Classes:** `CavemanError`, `ConfigError`, `ProviderError`, `RateLimitError`, `AuthError`, `ToolError`, `ToolNotFoundError`, `ToolPermissionError`, `ToolTimeoutError`, `MemoryError`, `BridgeError`, `SecurityError`

## `caveman.events`

Event bus — decouple everything from the agent loop. Core pattern: producers emit events, consumers subscribe. The agent loop ONLY emits. Display, logging, metrics, auditing, plugins all subscribe independently. Zero cou

**Classes:** `EventType`, `Event`, `EventBus`, `MetricsCollector`

**Functions:** `logging_subscriber()`, `create_default_bus()`

## `caveman.events_store`

EventStore — SQLite-backed event persistence for replay and audit. Every event emitted through the EventBus can be persisted here. Attach via ``bus.on_all(store.handle)`` for full capture. Replay: re-emit stored events t

**Classes:** `EventStore`

## `caveman.gateway.__init__`

Gateway — Discord, Telegram, and extensible chat interfaces.

## `caveman.gateway.access_control`

Access Control — user/channel/role permission management. Extracted from OpenClaw allow-list.ts (620 lines) and Hermes config-based access patterns. Features: - Multi-level access: owner → admin → user → guest - Channel 

**Classes:** `AccessLevel`, `AccessRule`, `ChannelConfig`, `AccessController`

## `caveman.gateway.acp_lifecycle`

ACP Lifecycle Commands — ACP session management from chat. Extracted from OpenClaw commands-acp/lifecycle.ts (868 lines). Handles: /acp spawn, /acp list, /acp send, /acp kill, /acp status.

**Classes:** `ACPSession`, `ACPLifecycleManager`

## `caveman.gateway.acp_lifecycle_depth`

ACP Lifecycle Depth — thread binding, persistent sessions, cleanup. Supplements acp_lifecycle.py with thread-bound sessions, persistence, and advanced cleanup. Extracted from OpenClaw ACP + sessions.

**Classes:** `ThreadBinding`, `ThreadBindingStore`, `PersistentSession`, `PersistentSessionStore`

## `caveman.gateway.agent_memory`

Agent Runner Memory — compaction, memory flush, token tracking. Extracted from OpenClaw agent-runner-memory.ts (848 lines). Manages context window budget, triggers compaction, and flushes memories.

**Classes:** `MemoryFlushPlan`, `TokenBudget`, `AgentMemoryManager`

**Functions:** `resolve_context_window()`

## `caveman.gateway.agent_memory_depth`

Agent Memory Depth — transcript estimation, memory flush, compaction. Supplements agent_memory.py with transcript token estimation, memory flush to disk, and compaction triggers. Extracted from OpenClaw agent-runner-memo

**Classes:** `MemoryFlushConfig`, `CompactionResult`

**Functions:** `estimate_tokens_for_model()`, `estimate_transcript_tokens()`, `get_context_window()`, `should_compact()`, `flush_transcript()`, `load_transcript()`, `prepare_compaction()`

## `caveman.gateway.agent_runner`

Agent Runner — core agent execution orchestration. Extracted from OpenClaw agent-runner.ts (874 lines). Orchestrates: session lookup, memory preflight, agent dispatch, reply routing.

**Classes:** `RunContext`, `RunResult`, `AgentRunner`

## `caveman.gateway.agent_runner_depth`

Agent Runner Depth — streaming output, tool progress, block reply. Supplements agent_runner.py with streaming support and tool execution status tracking. Extracted from OpenClaw agent-runner.ts (874 lines).

**Classes:** `ToolProgress`, `StreamEvent`, `StreamingAgentRunner`

## `caveman.gateway.allowlist_commands`

Allowlist Commands — user/channel/role access management. Extracted from OpenClaw commands-allowlist.ts (573 lines). Handles: /allowlist add, /allowlist remove, /allowlist list, pattern matching.

**Classes:** `AllowlistEntry`, `AllowlistManager`

## `caveman.gateway.allowlist_commands_depth`

Allowlist Commands Depth — bulk ops, pattern matching, import/export. Supplements allowlist_commands.py with bulk operations and pattern-based matching. Extracted from OpenClaw commands-allowlist.ts.

**Classes:** `AllowlistRule`

**Functions:** `bulk_add()`, `bulk_remove()`, `export_allowlist()`, `import_allowlist()`, `cleanup_expired()`

## `caveman.gateway.attachments`

Attachment Handler — image, file, and media processing. Handles attachment uploads, downloads, format conversion, and size validation for messaging platforms.

**Classes:** `Attachment`, `AttachmentHandler`

## `caveman.gateway.base`

Abstract Gateway base class.

**Classes:** `Gateway`

## `caveman.gateway.channel_directory`

Gateway channel directory — maps platform+chat_id to session metadata. Provides a unified lookup for which sessions are active on which platforms, enabling cross-platform features like session mirroring and message routi

**Classes:** `ChannelEntry`, `ChannelDirectory`

## `caveman.gateway.command_registry`

Command Registry — slash command system with auth, categories, and dispatch. Extracted from OpenClaw commands-registry.shared.ts (837 lines) and command-auth.ts (730 lines). Features: - Declarative command definition wit

**Classes:** `CommandCategory`, `CommandArg`, `CommandDefinition`, `ParsedCommand`, `CommandRegistry`

**Functions:** `build_builtin_commands()`

## `caveman.gateway.command_registry_depth`

Command Registry Depth — aliases, permissions, cooldowns, help. Supplements command_registry.py with command aliases, permission levels, cooldown tracking, and auto-help generation.

**Classes:** `CommandPermission`, `CommandCooldown`, `CommandAlias`, `EnhancedCommand`, `EnhancedCommandRegistry`

## `caveman.gateway.context_pruning`

Context Pruning — cache management and context window optimization. Manages conversation context to fit within model context windows, with intelligent pruning strategies and caching.

**Classes:** `PruningConfig`, `MessageImportance`, `ContextPruner`, `ContextCache`

## `caveman.gateway.cron_integration`

Cron integration for the gateway — runs cron jobs as agent tasks.

**Functions:** `start_cron_scheduler()`

## `caveman.gateway.daemon`

Daemon Manager — background process lifecycle management. Manages the caveman daemon process (start/stop/restart/status). Core patterns from OpenClaw src/daemon/ (10K LOC — extracted essentials).

**Classes:** `DaemonStatus`

**Functions:** `get_status()`, `start()`, `stop()`, `restart()`

## `caveman.gateway.debounce`

Inbound message debounce — merge rapid-fire messages into one task. When a user sends multiple messages quickly (common in Discord/Telegram), this module collects them and delivers a single merged task.

**Classes:** `MessageDebouncer`

## `caveman.gateway.directives`

Reply Directives — system directive parsing and resolution. Extracted from OpenClaw get-reply-directives.ts (581 lines). Parses inline directives from messages: /model, /reasoning, /verbose, etc.

**Classes:** `ParsedDirectives`, `DirectiveResolution`

**Functions:** `parse_inline_directives()`, `resolve_directives()`

## `caveman.gateway.directives_depth`

Directives Depth — /approve, /config, /help, /export, /import. Supplements directives.py with additional slash commands. Extracted from OpenClaw get-reply-directives.ts.

**Classes:** `ApproveDirective`, `ConfigDirective`, `ParsedDirective`, `ApprovalStore`

**Functions:** `parse_extended_directives()`, `generate_help()`

## `caveman.gateway.discord_adapter`

Discord Platform Adapter — BasePlatformAdapter implementation for Discord. This is the new-generation Discord adapter built on the unified platform abstraction layer. It replaces the legacy DiscordGateway for new deploym

**Classes:** `DiscordAdapter`

## `caveman.gateway.discord_buttons`

Discord button views — interactive UI components. Provides reusable button views for common interactions: - ConfirmView: Yes/No confirmation - ChoiceView: Multiple choice selection - PaginationView: Page through results

**Classes:** `ConfirmView`, `ChoiceView`, `PaginationView`

**Functions:** `send_with_buttons()`

## `caveman.gateway.discord_gw`

Discord gateway — receive tasks from Discord, run Caveman, reply. Trigger modes: - "all": respond to every message (like a chat assistant) - "prefix": only respond to !cave prefix or @mentions (default) - "thread": respo

**Classes:** `DiscordGateway`

## `caveman.gateway.discord_slash`

Discord slash command registration — extracted from discord_gw.py. Registers native Discord Application Commands (slash commands) including Chinese aliases for bilingual support.

**Functions:** `sync_slash_commands()`

## `caveman.gateway.dispatch`

Dispatch from Config — configuration-driven message routing. Extracted from OpenClaw dispatch-from-config.ts (1046 lines). Routes incoming messages through hooks, directives, and model dispatch.

**Classes:** `SendPolicy`, `DispatchContext`, `DispatchResult`, `HookRunner`, `MessageDispatcher`

**Functions:** `resolve_send_policy()`

## `caveman.gateway.dispatch_depth`

Dispatch Depth — streaming, TTS integration, block reply, tool status. Supplements dispatch.py with streaming output, TTS auto-apply, and block-based reply chunking. Extracted from OpenClaw dispatch-from-config.ts.

**Classes:** `StreamChunk`, `BlockReplyConfig`, `TTSConfig`, `StreamingDispatcher`

## `caveman.gateway.display_config`

Display configuration for gateway platforms. Controls how messages are formatted for different platforms: - Discord: Markdown, code blocks, embeds - Telegram: HTML or Markdown, inline keyboards - CLI: Rich panels, ANSI c

**Classes:** `Platform`, `DisplayConfig`

**Functions:** `get_display_config()`, `split_message()`

## `caveman.gateway.event_router`

Event Listeners — handle platform events beyond messages. Extracted from OpenClaw listeners.ts (774 lines) and Hermes event handling patterns. Features: - Reaction events (add/remove → command triggers) - Message edit/de

**Classes:** `EventType`, `PlatformEvent`, `EventRouter`

## `caveman.gateway.execution`

Agent Execution Engine — run agent with fallback, retry, error recovery. Extracted from OpenClaw agent-runner-execution.ts (1572 lines) and Hermes agent/ai_agent.py execution patterns. Features: - Model fallback chain (p

**Classes:** `FallbackCandidate`, `ExecutionConfig`, `ExecutionResult`, `AgentExecutionEngine`

## `caveman.gateway.feishu_adapter`

Feishu (Lark) Platform Adapter — BasePlatformAdapter implementation. Uses Feishu Open API for sending/receiving messages. Requires: APP_ID, APP_SECRET, and event subscription webhook. Features: - Text, image, file sendin

**Classes:** `FeishuAdapter`

## `caveman.gateway.flows`

Flows — multi-step workflow engine with conditions and parallel execution. Provides a DAG-based workflow engine for orchestrating complex multi-step agent tasks with branching, loops, and parallel execution. Inspired by 

**Classes:** `StepStatus`, `StepResult`, `FlowStep`, `Flow`, `FlowEngine`, `FlowStatus`, `ProviderSetupFlow`

**Functions:** `create_flow()`, `list_flows()`

## `caveman.gateway.gateway_lifecycle`

Gateway process lifecycle — startup, SIGUSR1 restart, drain, and shutdown. Orchestrates the full gateway process lifecycle: 1. Environment sanitization 2. Restart sentinel check (was this a restart?) 3. PID file manageme

**Functions:** `drain_active_sessions()`, `run_gateway_forever()`

## `caveman.gateway.health`

Health check endpoint — lightweight HTTP server for monitoring. Provides /health and /status endpoints for: - Container orchestration (k8s liveness/readiness probes) - External monitoring (uptime checks) - Gateway diagno

**Classes:** `HealthServer`

## `caveman.gateway.hooks`

User-defined event hook system. Discovers hooks from ~/.caveman/hooks/ directories, each containing: - HOOK.yaml (metadata: name, description, events list) - handler.py (Python handler with async def handle(event_type, c

**Classes:** `HookRegistry`

## `caveman.gateway.infra`

Gateway infrastructure — hooks, task registry, and pipeline integration. Extracted from runner.py to keep it under 450 lines. Provides lazy-loaded singletons for hooks and task registry.

**Classes:** `GatewayInfra`

## `caveman.gateway.interactive`

Interactive — buttons, forms, and selection UI for chat platforms. Provides platform-agnostic interactive components that render as inline buttons (Discord), reply keyboards (Telegram), or text menus (fallback). Extracte

**Classes:** `Button`, `ButtonRow`, `SelectOption`, `SelectMenu`, `FormField`, `Form`, `InteractiveMessage`, `InteractionEvent`, `ButtonStyle`

**Functions:** `render_text_fallback()`, `render_discord_components()`, `to_discord_components()`, `to_telegram_keyboard()`

## `caveman.gateway.legacy_startup`

Legacy gateway startup — backward-compatible Gateway ABC path. Used when use_platform_adapters=False or as fallback when no new adapters match. Will be removed once all platforms are migrated to BasePlatformAdapter.

**Functions:** `start_legacy_gateways()`

## `caveman.gateway.link_understanding`

Link understanding — auto-detect URLs in messages and fetch content. When a user sends a message containing URLs, this module: 1. Extracts URLs from the message text 2. Fetches readable content from each URL (with timeou

**Functions:** `extract_urls()`, `format_link_context()`, `fetch_url_content()`, `understand_links()`

## `caveman.gateway.matrix_adapter`

Matrix Platform Adapter — BasePlatformAdapter implementation for Matrix. Uses matrix-nio for the Matrix protocol. Features: E2EE support, room management, media uploads, reactions.

**Classes:** `MatrixAdapter`

## `caveman.gateway.media_cache`

Sticker/media cache — avoid re-downloading the same media. Caches downloaded stickers, images, and other media files by content hash. Reduces bandwidth and latency for repeated media in chat platforms.

**Classes:** `MediaCache`

## `caveman.gateway.message_pipeline`

Inbound message pipeline — the heart of Caveman's message processing. Inspired by OpenClaw's auto-reply architecture (45K LOC), distilled to essentials. Pipeline stages: 1. Normalize — clean text, extract metadata, sanit

**Classes:** `MessageAction`, `MessageContext`, `DedupeCache`

**Functions:** `normalize_message()`, `dedupe_message()`, `preprocess_message()`, `route_message()`, `execute_message()`, `prepare_delivery()`, `process_inbound()`

## `caveman.gateway.message_splitting`

Message Splitting — platform-aware message chunking. Splits long messages to fit platform character limits while preserving code blocks, markdown, and natural break points.

**Functions:** `split_message()`, `estimate_message_count()`

## `caveman.gateway.mirror`

Session mirroring for cross-platform message delivery. When a message is sent via CLI, cron, or a different gateway, this module appends a "delivery-mirror" record to the target session's transcript so the receiving-side

**Functions:** `mirror_to_session()`

## `caveman.gateway.mock_agent`

Mock agent and components for gateway slash command handling. Provides all attributes accessed by command handlers so slash commands return meaningful data instead of dummy values.

**Classes:** `GatewayMockAgent`

## `caveman.gateway.model_selector`

Model Selector — per-channel, per-user, fallback model resolution. Extracted from OpenClaw model-picker.ts (946 lines) and Hermes model selection patterns. Features: - Per-channel model override - Per-user model preferen

**Classes:** `ModelEntry`, `ModelSelector`

## `caveman.gateway.outbound`

Outbound Message Delivery — chunk, format, and deliver responses. Extracted from OpenClaw send.outbound.ts (601 lines) and Hermes BasePlatformAdapter._send_with_retry + truncate_message. Features: - Code-block-aware chun

**Classes:** `ReactionState`, `OutboundDelivery`

**Functions:** `chunk_message()`, `strip_markdown()`

## `caveman.gateway.pairing`

Gateway device pairing — QR code / token based pairing for mobile apps. Generates a pairing token, displays it (or QR code), and waits for a device to claim it. Once paired, the device can send/receive messages through t

**Classes:** `PairedDevice`, `PairingToken`, `PairingManager`

## `caveman.gateway.platform_adapter`

Base Platform Adapter — unified abstraction for all messaging platforms. Subclasses implement connect/disconnect/send. Base handles: typing, retry, media extraction, interrupt, session tracking, message splitting.

**Classes:** `BasePlatformAdapter`

## `caveman.gateway.platform_delivery`

Platform delivery utilities — media extraction, retry, message splitting. Extracted from platform_adapter.py to keep it under 450 lines. These are static/standalone utilities used by BasePlatformAdapter.

**Functions:** `extract_media()`, `extract_images()`, `is_animation_url()`, `truncate_message()`

## `caveman.gateway.platform_registry`

Platform Registry — discover and instantiate platform adapters. Central registry for all available platform adapters. Used by the gateway runner to start configured platforms. Usage: from caveman.gateway.platform_registr

**Functions:** `register_adapter()`, `get_adapter()`, `list_platforms()`

## `caveman.gateway.platform_types`

Platform types — shared data structures for the gateway layer. All platform adapters produce/consume these types. They are the contract between the gateway runner and individual platform implementations.

**Classes:** `Platform`, `MessageType`, `ProcessingOutcome`, `SessionSource`, `MessageEvent`, `SendResult`, `PlatformConfig`

**Functions:** `build_session_key()`

## `caveman.gateway.preflight`

Message Preflight — filter, validate, and enrich incoming messages. Extracted from OpenClaw message-handler.preflight.ts (1124 lines) and Hermes BasePlatformAdapter.handle_message (400 lines). Pipeline: self-filter → bot

**Classes:** `PreflightConfig`, `QuotedMessage`, `PreflightResult`, `MessagePreflight`

## `caveman.gateway.processor`

Message Processing Engine — handle messages end-to-end. Extracted from Hermes _process_message_background (270 lines) and OpenClaw processDiscordMessage (900 lines). Handles: session management → typing → agent call → me

**Classes:** `MessageProcessor`

**Functions:** `extract_media_tags()`, `extract_images()`, `extract_local_files()`

## `caveman.gateway.processor_depth`

Processor Depth — streaming output, tool progress, retry logic. Supplements processor.py with streaming support and tool execution progress tracking. Extracted from OpenClaw gateway processor.

**Classes:** `ToolProgressEvent`, `RetryConfig`, `StreamingProcessor`

## `caveman.gateway.rate_limiter`

Rate Limiter — token bucket algorithm for API rate limiting. Provides per-key rate limiting with token bucket algorithm, sliding window, and burst support.

**Classes:** `TokenBucket`, `RateLimiter`

## `caveman.gateway.redaction`

Redaction Engine — PII and secret detection/masking. Detects and redacts sensitive information from text before it's sent to external services or displayed. Extracted from Hermes agent/redact.py + OpenClaw redaction patt

**Functions:** `redact_secrets()`, `redact_pii()`, `redact_all()`, `detect_secrets()`, `detect_pii()`, `is_safe_for_external()`

## `caveman.gateway.reply_queue`

Reply queue — buffer and drain followup messages. When multiple messages arrive while the agent is processing, they're queued and drained in order after the current run completes. Prevents message loss and ensures sequen

**Classes:** `QueuePolicy`, `QueuedMessage`, `ReplyQueue`, `QueueManager`

## `caveman.gateway.restart`

Gateway graceful restart — SIGUSR1-based restart with drain support. Restart flow: 1. /restart command or SIGUSR1 signal received 2. Set _restart_requested flag 3. Drain active agents (wait for completion, up to timeout)

**Functions:** `parse_drain_timeout()`, `request_restart_via_signal()`, `request_restart_via_launchd()`, `request_restart_via_systemd()`, `trigger_restart()`, `write_restart_sentinel()`, `consume_restart_sentinel()`

## `caveman.gateway.router`

Gateway router — route messages across multiple gateways.

**Classes:** `GatewayRouter`

## `caveman.gateway.routing`

Message routing — resolve which agent/session handles a message. Maps inbound messages to the correct agent session based on: - Platform + chat_id → session binding - Account ID resolution - Thread routing - Multi-agent 

**Classes:** `Route`, `SessionBinding`

**Functions:** `resolve_route()`, `parse_session_key()`, `resolve_account_id()`

## `caveman.gateway.runner`

Gateway runner — streaming session management for Discord/Telegram.

**Classes:** `GatewayServer`

**Functions:** `run_gateway()`, `run_gateway_forever()`

## `caveman.gateway.secrets`

Secrets Manager — credential storage, rotation, and resolution. Provides secure credential management with multiple backends, rotation support, and environment variable resolution. Core patterns from OpenClaw src/secrets

**Classes:** `Credential`, `SecretsManager`

## `caveman.gateway.security_audit`

Security Audit — system security scanning and policy enforcement. Provides security auditing for the agent system including file permissions, network exposure, and configuration safety. Core patterns from OpenClaw src/se

**Classes:** `AuditFinding`, `AuditReport`

**Functions:** `run_audit()`

## `caveman.gateway.service`

Service installation — launchd (macOS) and systemd (Linux) support. Provides `caveman install` and `caveman uninstall` CLI commands to register the gateway as a system service with automatic restart on crash. macOS: ~/Li

**Functions:** `generate_launchd_plist()`, `install_launchd()`, `uninstall_launchd()`, `generate_systemd_unit()`, `install_systemd()`, `uninstall_systemd()`, `install_service()`, `uninstall_service()`

## `caveman.gateway.session_commands`

Session Commands — session lifecycle management. Extracted from OpenClaw commands-session.ts (669 lines). Handles: /reset, /compact, /session idle, /session max-age, session binding.

**Classes:** `SessionBinding`, `SessionCommandHandler`

**Functions:** `parse_duration_ms()`

## `caveman.gateway.session_commands_depth`

Session Commands Depth — export, import, search, bulk ops. Supplements session_commands.py with session export/import, search, and bulk operations. Extracted from OpenClaw commands-session.ts.

**Classes:** `SessionExport`

**Functions:** `export_session()`, `import_session()`, `search_sessions()`, `bulk_delete_sessions()`

## `caveman.gateway.session_context`

Session-scoped context variables for the gateway. Replaces os.environ-based session state with Python's contextvars.ContextVar. Each asyncio task gets its own copy, so concurrent messages never interfere.

**Functions:** `set_session_context()`, `get_session_env()`, `get_session_context()`, `clear_session_context()`

## `caveman.gateway.session_manager`

Session Manager — deep session lifecycle with context management. Extracted from OpenClaw auto-reply/reply/session.ts (792 lines) and Hermes gateway/session_manager.py patterns. Features: - Context window tracking and au

**Classes:** `SessionEntry`, `GatewaySessionManager`

## `caveman.gateway.signal_adapter`

Signal Platform Adapter — BasePlatformAdapter implementation for Signal. Uses signal-cli (REST API mode) for sending/receiving messages. Requires: signal-cli running in JSON-RPC or REST mode. Features: - Text, image, aud

**Classes:** `SignalAdapter`

## `caveman.gateway.slack_adapter`

Slack Platform Adapter — BasePlatformAdapter implementation for Slack. Features: - Slack Bolt SDK integration - Socket Mode (no public URL needed) - Thread support - Block Kit formatting - File uploads - Emoji reactions

**Classes:** `SlackAdapter`

## `caveman.gateway.slack_gw`

Slack gateway — receive tasks from Slack, run Caveman, reply. Uses Slack's Socket Mode (no public URL needed) or Events API. Requires: slack_sdk (pip install slack-sdk)

**Classes:** `SlackGateway`

## `caveman.gateway.smart_buffer`

SmartBuffer — Three-layer text buffer for gateway streaming. Hermes-inspired architecture: - Layer 1: _full_text — all text for conversation history - Layer 2: interim flush — text before tool_calls sent as commentary - 

## `caveman.gateway.status`

Gateway process status — PID file, runtime state, and process identity. Provides PID-file based detection of whether the gateway is running, runtime state tracking (starting/running/draining/stopped), and process identit

**Functions:** `write_pid_file()`, `remove_pid_file()`, `get_running_pid()`, `is_gateway_running()`, `write_runtime_state()`, `read_runtime_state()`, `terminate_pid()`

## `caveman.gateway.status_depth`

Status Panel Depth — token stats, model info, session list, cost tracking. Supplements status_panel.py with detailed token/cost tracking and session listing. Extracted from OpenClaw status.ts (930 lines).

**Classes:** `TokenStats`, `ModelInfo`, `SessionListEntry`

**Functions:** `get_model_info()`, `format_session_list()`, `format_token_stats()`, `format_model_info()`

## `caveman.gateway.status_panel`

Status Panel — session and system status display. Extracted from OpenClaw status.ts (930 lines). Generates rich status panels for CLI, Discord, and Telegram.

**Classes:** `SessionStatus`, `SystemStatus`

**Functions:** `build_system_status()`, `format_status_text()`, `format_status_embed()`

## `caveman.gateway.stream_consumer`

Gateway stream consumer — bridges sync agent callbacks to async platform delivery. The agent fires stream_delta_callback(text) synchronously from its worker thread. StreamConsumer: 1. Receives deltas via on_delta() (thre

**Classes:** `StreamConfig`, `StreamConsumer`

## `caveman.gateway.task_registry`

Task registry — persistent task tracking with flows and delivery. Inspired by OpenClaw's task system (6.5K LOC), distilled to essentials. A Task represents a unit of work (agent run, sub-agent, cron job, etc.) with lifec

**Classes:** `TaskStatus`, `TaskRuntime`, `DeliveryStatus`, `TaskRecord`, `TaskFlow`, `TaskRegistry`

## `caveman.gateway.task_runner`

Task runner — executes a single agent task with activity monitoring. Implements: - Activity-based idle detection (replaces hard timeout) - Stuck-loop detection (same tool+args repeated N times) - Graceful shutdown (flag-

**Classes:** `AgentTaskError`

**Functions:** `run_single_task()`

## `caveman.gateway.task_runner_helpers`

Task runner helper functions. Internal helpers for task execution: activity monitoring, tool call handling, result persistence, and post-task review spawning.

## `caveman.gateway.telegram_adapter`

Telegram Platform Adapter — BasePlatformAdapter implementation for Telegram. Features: - python-telegram-bot 20.x integration - MarkdownV2 formatting with safe fallback - Voice message support (OGG/Opus) - Photo/video/do

**Classes:** `TelegramAdapter`

**Functions:** `format_markdown_v2()`

## `caveman.gateway.telegram_gw`

Telegram gateway — receive tasks from Telegram, run Caveman, reply.

**Classes:** `TelegramGateway`

## `caveman.gateway.threading`

Thread Manager — create, bind, and manage conversation threads. Extracted from OpenClaw threading.ts (678 lines) and Hermes thread_bindings.manager.ts (740 lines). Features: - Auto-thread creation for long conversations 

**Classes:** `ThreadBinding`, `AutoThreadConfig`, `ThreadManager`

**Functions:** `sanitize_thread_name()`

## `caveman.gateway.webhook`

Webhook Server — HTTP webhook endpoint for external integrations. Provides a lightweight webhook server that can receive events from external services and route them to the agent. Extracted from Hermes hermes_cli/webhook

**Classes:** `WebhookSubscription`, `WebhookManager`

## `caveman.gateway.whatsapp_adapter`

WhatsApp Platform Adapter — BasePlatformAdapter implementation for WhatsApp. Uses the WhatsApp Business API (Cloud API) via HTTP. Requires: WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN. Features: - Tex

**Classes:** `WhatsAppAdapter`

## `caveman.gateway.wiring`

Gateway wiring — connects all gateway subsystems into the live server. Called once during GatewayServer.start() to wire orphan modules. Each subsystem is imported in a try/except so failures are non-fatal.

**Functions:** `wire_gateway()`

## `caveman.hub.__init__`

Hub — skill marketplace client.

## `caveman.hub.client`

Caveman Hub — skill & plugin marketplace client. Hub API (future): GET /api/skills — List published skills GET /api/skills/{name} — Get skill details POST /api/skills — Publish a skill GET /api/plugins — List plugins GET

**Classes:** `HubClient`, `MigrationTool`

## `caveman.import_.__init__`

Caveman Import System — modular importers for external memory sources.

## `caveman.import_.base`

Base classes for the import system — ABC, dataclasses, type inference.

**Classes:** `ImportItem`, `ImportManifest`, `ImportResult`, `BaseImporter`

**Functions:** `infer_type()`, `split_markdown_sections()`, `write_import_log()`

## `caveman.import_.claude_code`

Claude Code importer — settings.json + plans/*.md + project CLAUDE.md files.

**Classes:** `ClaudeCodeImporter`

## `caveman.import_.codex`

Codex importer — MEMORY.md + rollout_summaries/.

**Classes:** `CodexImporter`

## `caveman.import_.config_merger`

Config merger — extract useful settings from external configs.

**Classes:** `ConfigMerger`

## `caveman.import_.dedup`

Content-hash based deduplication for imports.

**Classes:** `ImportDedup`

**Functions:** `content_hash()`

## `caveman.import_.directory`

Directory importer — generic import from any directory of .md files.

**Classes:** `DirectoryImporter`

## `caveman.import_.hermes`

Hermes importer — § (section sign) delimited memories + nested skills.

**Classes:** `HermesImporter`

**Functions:** `split_by_section_sign()`

## `caveman.import_.obsidian_bridge`

Obsidian Vault Bridge — live sync between Obsidian and Caveman's memory. See obsidian_models.py for data models (ObsidianNote, ObsidianVault, VaultSyncState, SyncResult) and parsing utilities.

**Classes:** `ObsidianBridge`

## `caveman.import_.obsidian_models`

Obsidian Vault Bridge — live sync between Obsidian and Caveman's memory. Architecture decision (长期主义 + 最高复利): NOT a one-time import. That's "够用就行" thinking. Instead: Obsidian vault becomes a **live knowledge source** for

**Classes:** `ObsidianNote`, `ObsidianVault`, `FileSyncRecord`, `VaultSyncState`, `SyncResult`

**Functions:** `parse_note()`

## `caveman.import_.openclaw`

OpenClaw importer — full recursive scan of ~/.openclaw/.

**Classes:** `OpenClawImporter`

## `caveman.import_.openclaw_sessions`

OpenClaw Session Importer — extract knowledge from session conversations. This is a separate importer from OpenClawImporter because: 1. Session extraction is opt-in (user decides per session) 2. It can use LLM for higher

**Classes:** `OpenClawSessionImporter`

## `caveman.import_.report`

Import report generator — pretty terminal output.

**Functions:** `format_manifest_report()`, `format_result_report()`, `format_detect_report()`

## `caveman.import_.session_extractor`

Session Knowledge Extractor — extraction stage of the knowledge pipeline. Uses scoring and clustering from session_scoring.py, then applies rule-based or LLM-based extraction to produce structured knowledge entries.

**Classes:** `ExtractedKnowledge`, `ExtractionResult`

**Functions:** `extract_knowledge_rule_based()`, `extract_knowledge_with_llm()`, `extract_session()`

## `caveman.import_.session_parser`

OpenClaw Session JSONL Parser — structured extraction of conversation data. Parses the JSONL format used by OpenClaw's session storage: - Each line is a JSON object with a `type` field - Types: session, message, model_ch

**Classes:** `ToolCall`, `ConversationTurn`, `SessionMetadata`, `ParsedSession`

**Functions:** `parse_session()`, `scan_sessions()`

## `caveman.import_.session_scoring`

Session Knowledge Extractor — distill high-value knowledge from conversations. Three-stage pipeline: 1. **Filter**: Score each turn for knowledge density, discard noise 2. **Cluster**: Group related turns into coherent k

**Classes:** `KnowledgeCategory`, `TurnScore`, `KnowledgeCluster`

**Functions:** `score_turn()`, `cluster_turns()`

## `caveman.import_.workspace_adapter`

Workspace file adapter — transforms OpenClaw workspace files for Caveman. When importing from OpenClaw, workspace files (SOUL.md, AGENTS.md, etc.) contain references to OpenClaw-specific tools, concepts, and patterns. Th

**Functions:** `adapt_workspace_content()`, `validate_adapted_content()`

## `caveman.lifecycle`

Lifecycle manager — unified startup, shutdown, and resource cleanup. Problem: Resources scattered across modules (SQLite, browser, UDS server, gateways) with no coordinated startup/shutdown order. Ctrl+C kills without cl

**Classes:** `Lifecycle`

## `caveman.logging_config`

Structured JSON log formatter for production deployments. Usage: In config.yaml: logging: format: json # or "text" (default) Or programmatically: from caveman.logging_config import setup_logging setup_logging(format="jso

**Classes:** `JsonFormatter`

**Functions:** `setup_logging()`

## `caveman.mcp.__init__`

Caveman MCP — expose the flywheel as MCP tools for any agent.

## `caveman.mcp.__main__`

Allow running as: python -m caveman.mcp.server

## `caveman.mcp.client`

MCP Client — connect to external MCP servers and use their tools. Supports stdio (spawn process) and SSE (HTTP) transports.

**Classes:** `MCPClient`

## `caveman.mcp.manager`

MCP Manager — manage multiple MCP server connections.

**Classes:** `MCPManager`

## `caveman.mcp.oauth`

MCP OAuth authentication for MCP server connections. Handles OAuth 2.0 flows for MCP servers that require authentication. Supports authorization code flow with PKCE.

**Functions:** `generate_pkce()`, `build_auth_url()`, `refresh_token()`, `save_tokens()`, `load_tokens()`, `is_token_expired()`

## `caveman.mcp.server`

Caveman MCP Server — expose the 6-engine flywheel as MCP tools. Any MCP-compatible agent (Claude Code, Codex, Gemini CLI) can use Caveman's memory, recall, shield, and reflect capabilities without switching tools. Usage:

**Functions:** `memory_store()`, `memory_search()`, `memory_recall()`, `shield_save()`, `shield_load()`, `reflect()`, `skill_list()`, `skill_get()`, `wiki_compile()`, `wiki_ingest()`, `get_status()`, `run_stdio()`, `run_http()`

## `caveman.memory.__init__`

Memory subsystem — 4-type memory with drift detection and refinement.

## `caveman.memory.backend`

MemoryBackend Protocol — unified interface for pluggable memory storage. Unifies MemoryManager (JSON) and SQLiteMemoryStore behind a single interface. Default backend: SQLite + FTS5. JSON kept as fallback.

**Classes:** `MemoryBackend`

## `caveman.memory.confidence`

DEPRECATED: Legacy confidence scoring — replaced by trust_score in sqlite_store.py. Scheduled for removal in v0.5.0. See PRD §8.9.2 line 1073. Use sqlite_store.mark_helpful() + retrieval.adjust_trust() instead. Original 

**Classes:** `ConfidenceRecord`, `ConfidenceTracker`

## `caveman.memory.decay`

Memory decay — time-based trust erosion for unused memories. The flywheel's garbage collector. Without decay, low-quality memories accumulate forever, polluting retrieval results and wasting resources. Decay rules (compo

**Classes:** `DecayResult`, `MemoryDecay`

## `caveman.memory.drift`

Memory Drift Detection — detect and resolve conflicting memories. Principle from Claude Code: "Memory says X exists ≠ X exists now. Verify before use." Drift types: - Contradiction: "user likes A" vs "user dislikes A" (s

**Classes:** `DriftDetector`

## `caveman.memory.embedding`

Embedding providers — generate vector embeddings for memory search.

**Functions:** `fastembed_embedding()`, `openai_embedding()`, `ollama_embedding()`, `local_embedding()`, `get_embedding_fn()`

## `caveman.memory.flywheel_metrics`

Flywheel health metrics — quantify whether the flywheel is turning. Key metrics: - trust_distribution: histogram of trust scores (healthy = bell curve, not flat) - feedback_rate: % of recalled memories that got trust fee

**Classes:** `FlywheelHealth`

## `caveman.memory.grounding`

Memory grounding — verify memories against current reality.

**Functions:** `ground()`

## `caveman.memory.judge`

LLM judge for memory quality and helpfulness feedback. PRD Round 14: replace fragile heuristics with an auditable LLM judge while keeping deterministic fallbacks for offline/test environments.

**Classes:** `JudgeResult`, `MemoryJudge`

**Functions:** `heuristic_helpfulness()`

## `caveman.memory.manager`

Memory manager v3 — pluggable backend with SQLite default. Breaking change from v2: MemoryManager now delegates to a MemoryBackend. Default backend: SQLiteMemoryStore (FTS5 + hybrid retrieval). Fallback: in-memory JSON (

**Classes:** `MemoryManager`

## `caveman.memory.metadata`

Memory metadata contract validation. Well-known metadata keys are intentionally permissive about unknown keys, but known keys must have stable types because retrieval, lint, ripple and decay engines read them directly. I

**Classes:** `MetadataSpec`

**Functions:** `validate_metadata()`

## `caveman.memory.nudge`

Memory nudge — background extraction of memories from conversations. Every N turns, a background "nudge" reviews the conversation and extracts valuable information into long-term memory. Inspired by Hermes's _spawn_backg

**Classes:** `MemoryNudge`

## `caveman.memory.obsidian`

Obsidian-compatible memory export. Exports Caveman's Memory Store as Obsidian-compatible markdown files with wikilinks, tags, and YAML frontmatter. Output structure: obsidian_vault/ memories/ semantic/ server-ip-39-99-23

**Functions:** `export_to_obsidian()`

## `caveman.memory.provider`

Memory provider abstraction — pluggable backends. .. deprecated:: 0.3.0 This module is dead code. The production MemoryManager lives in caveman.memory.manager. Will be removed in 0.4.0. Ported from Hermes MemoryProvider 

**Classes:** `MemoryProvider`, `BuiltinMemoryProvider`

## `caveman.memory.quality_gate`

Memory quality gate — prevents garbage from entering the flywheel. This is the highest-leverage intervention in the entire system. Every piece of garbage that enters costs: - Storage space forever - FTS5 index pollution 

**Classes:** `QualityGateStats`

**Functions:** `check_quality()`, `check_quality_async()`, `get_stats()`, `truncate_if_needed()`, `reset_cache()`

## `caveman.memory.recall_cache`

LRU recall cache with TTL expiration.

**Classes:** `RecallCache`

## `caveman.memory.refiner`

Nudge Phase 2 — LLM-powered memory refinement. Takes raw Phase 1 memories and refines them: - Deduplication: merge near-identical entries - Conflict detection: flag contradictions with existing memories - Quality upgrade

**Classes:** `RefinementResult`, `NudgeRefiner`

## `caveman.memory.retrieval`

Hybrid retrieval — FTS5 + Jaccard + vector + trust + temporal decay. Ported from Hermes FactRetriever (MIT, Nous Research) with Caveman adaptations. Replaces simple FTS5/vector search with multi-signal scoring pipeline.

**Classes:** `HybridScorer`

**Functions:** `tokenize()`, `jaccard_similarity()`, `extract_entities()`, `temporal_decay()`, `adjust_trust()`, `expand_query_cross_lang()`

## `caveman.memory.security`

Memory content security scanner. Ported from Hermes memory_tool.py (MIT, Nous Research). Scans memory content for injection, exfiltration, and deception patterns before storing. Memories are injected into system prompts,

**Functions:** `scan_memory_content()`, `is_safe()`

## `caveman.memory.sqlite_store`

SQLite + FTS5 memory backend.

**Classes:** `SQLiteMemoryStore`

## `caveman.memory.store_helpers`

SQLite memory store helpers — extracted to keep sqlite_store.py under 400 lines. Schema versioning (PRD §8.9.6 + §8.12): Current: SCHEMA_VERSION = 2 Migration: schema_version table + numbered, transactional migration fun

**Functions:** `get_schema_version()`, `pending_migrations()`, `row_to_entry()`, `migrate_schema()`

## `caveman.memory.types`

Memory type definitions. Two classification dimensions (PRD §4.1 + §5.2): - MemoryType: cognitive type (how the memory is structured) - MemorySource: PRD source category (where the memory came from) Storage uses MemoryTy

**Classes:** `MemoryType`, `MemorySource`, `MemoryEntry`, `MetadataKeys`

**Functions:** `resolve_memory_type()`, `get_turn_text()`

## `caveman.paths`

Centralized constants — single source of truth for paths, defaults, and magic numbers. All hardcoded "~/.caveman/..." paths, timeout values, token limits, and other repeated constants should reference this module. Changi

**Functions:** `ensure_home()`

## `caveman.plugins.__init__`

Plugin system — extensible agent capabilities.

## `caveman.plugins.manager`

Plugin system — extensible capability loading. Plugins extend Caveman with: - New tools (browser automation, database, API clients) - New providers (local models, alternative APIs) - New skills (domain-specific expertise

**Classes:** `PluginMeta`, `PluginManager`

## `caveman.providers.__init__`

LLM providers — Anthropic, OpenAI, Gemini, Ollama, Groq, DeepSeek, Mistral, Together, unified interface, and infrastructure.

## `caveman.providers.anthropic_adapter`

Anthropic message format adapter — converts between OpenAI and Anthropic formats. Ported from Hermes anthropic_adapter.py (MIT, Nous Research). Handles: role alternation, tool_use/tool_result pairing, thinking block mana

**Functions:** `get_max_output()`, `supports_adaptive_thinking()`, `build_api_kwargs()`

## `caveman.providers.anthropic_messages`

Anthropic message format conversion helpers. Pure functions for converting between Caveman's internal message format and the Anthropic API's expected format.

**Functions:** `convert_tools()`, `convert_messages()`

## `caveman.providers.anthropic_provider`

Anthropic provider — production-grade Claude API with streaming + tool use. Integrates: anthropic_adapter (message conversion), error_classifier (smart retry), retry (jittered backoff), rate_limit (header tracking), prom

**Classes:** `AnthropicProvider`

## `caveman.providers.credential_pool`

Multi-credential pool with rotation strategies. Inspired by Hermes credential_pool.py (MIT, Nous Research). Simplified for Caveman — manages multiple API keys per provider with automatic cooldown on exhaustion.

**Classes:** `RotationStrategy`, `Credential`, `CredentialPool`

## `caveman.providers.deepseek_provider`

DeepSeek provider — OpenAI-compatible API at api.deepseek.com.

**Classes:** `DeepSeekProvider`

## `caveman.providers.error_classifier`

API error classification for smart recovery. Ported from Hermes error_classifier.py (MIT, Nous Research). Classifies API errors into structured recovery recommendations.

**Classes:** `FailoverReason`, `ClassifiedError`

**Functions:** `classify_error()`

## `caveman.providers.fallback_chain`

Model fallback chain — automatic failover when primary model fails. Ported from Hermes _try_activate_fallback (MIT, Nous Research). Config example: agent: fallback_chain: - provider: anthropic model: claude-sonnet-4-2025

**Classes:** `FallbackEntry`, `FallbackChain`

## `caveman.providers.gemini_provider`

Gemini provider — Google Generative AI REST API with streaming + tool use. Uses httpx to call the Gemini REST API directly (no SDK dependency). Integrates error_classifier for smart retry decisions.

**Classes:** `GeminiProvider`

## `caveman.providers.groq_provider`

Groq provider — OpenAI-compatible API at api.groq.com.

**Classes:** `GroqProvider`

## `caveman.providers.insights`

Usage tracking and cost estimation. Inspired by Hermes InsightsEngine (MIT, Nous Research). Simplified for Caveman — tracks tokens, costs, and session stats.

**Classes:** `TurnUsage`, `SessionInsights`

## `caveman.providers.llm`

LLM provider abstraction — unified interface across providers. All providers yield normalized event dicts: - {"type": "delta", "text": "..."} - {"type": "tool_call", "id": "...", "name": "...", "input": {...}} - {"type":

**Classes:** `LLMProvider`

**Functions:** `normalize_stop_reason()`

## `caveman.providers.message_sanitizer`

Pre-API message sanitization — fix orphaned tool calls before every LLM call. Ported from Hermes _sanitize_api_messages (MIT, Nous Research). Runs unconditionally before API calls to catch orphans from: - Context compres

**Functions:** `sanitize_messages()`, `deduplicate_tool_calls()`, `sanitize_surrogates()`, `strip_reasoning_tags()`

## `caveman.providers.mistral_provider`

Mistral provider — OpenAI-compatible API at api.mistral.ai.

**Classes:** `MistralProvider`

## `caveman.providers.model_metadata`

Model metadata — context lengths, pricing, and provider detection. Ported from Hermes agent/model_metadata.py (1085 lines → ~350 lines). Keeps the essential: model database, provider prefix stripping, context length dete

**Classes:** `ModelInfo`

**Functions:** `strip_provider_prefix()`, `get_model_info()`, `infer_provider_from_url()`, `is_local_endpoint()`, `detect_local_server_type()`

## `caveman.providers.model_router`

Smart model routing — cheap model for simple turns, strong for complex. Ported from Hermes smart_model_routing.py (MIT, Nous Research). Conservative: if any sign of complexity, keep the primary model.

**Functions:** `is_simple_turn()`, `choose_model()`

## `caveman.providers.ollama_provider`

Ollama LLM Provider — local model inference. Connects to a local Ollama instance for: 1. Running fine-tuned embedding models (training loop closure) 2. Running local LLMs for cost-free inference 3. Offline operation Uses

**Classes:** `OllamaProvider`

## `caveman.providers.openai_messages`

Convert Caveman internal message format → OpenAI Chat API format. Caveman stores messages in Anthropic-style content blocks: assistant: [{"type": "text", ...}, {"type": "tool_use", ...}] user/tool: [{"type": "tool_result

**Functions:** `convert_to_openai_messages()`

## `caveman.providers.openai_provider`

OpenAI provider — GPT/compatible API with streaming + tool use. Supports OpenAI, Azure, and any OpenAI-compatible endpoint. Integrates error_classifier for smart retry decisions.

**Classes:** `OpenAIProvider`

## `caveman.providers.openrouter_provider`

OpenRouter provider — access 200+ models through a single API. OpenRouter provides a unified API compatible with OpenAI's format, giving access to models from Anthropic, Google, Meta, Mistral, etc.

**Classes:** `OpenRouterProvider`

## `caveman.providers.prompt_cache`

Anthropic prompt caching — system_and_3 strategy. Ported from Hermes prompt_caching.py (MIT, Nous Research). Reduces input token costs ~75% by caching conversation prefix. Uses 4 cache_control breakpoints (Anthropic max)

**Functions:** `apply_cache_control()`

## `caveman.providers.rate_limit`

Rate limit tracking from API response headers. Ported from Hermes rate_limit_tracker.py (MIT, Nous Research). Captures x-ratelimit-* headers and provides formatted display. Integrates with LLM Scheduler for dynamic rate 

**Classes:** `RateLimitBucket`, `RateLimitState`

**Functions:** `parse_rate_limit_headers()`, `format_rate_limits()`, `format_compact()`

## `caveman.providers.registry`

Provider Registry — declarative provider resolution. Replaces the 80-line if-elif chain in factory.py with a data-driven registry. Each provider declares its model patterns and env var.

**Classes:** `ProviderSpec`

**Functions:** `resolve_provider()`

## `caveman.providers.retry`

Retry engine with jittered exponential backoff. Ported from Hermes retry_utils.py (MIT, Nous Research). Integrates with ErrorClassifier for smart recovery decisions.

**Functions:** `jittered_backoff()`, `retry_with_backoff()`

## `caveman.providers.together_provider`

Together AI provider — OpenAI-compatible API at api.together.xyz.

**Classes:** `TogetherProvider`

## `caveman.providers.usage_pricing`

Usage tracking and cost estimation. Ported from Hermes agent/usage_pricing.py (632 lines → ~200 lines). Tracks per-session token usage and estimates costs using model metadata.

**Classes:** `TurnUsage`, `SessionUsage`, `UsageTracker`

## `caveman.result`

Structured result types — consistent return shapes across the entire framework. Problem: 21+ places return ad-hoc {"error": ...} dicts with inconsistent keys. Solution: ToolResult/Result as the universal result envelope.

**Classes:** `ToolResult`

**Functions:** `Ok()`, `Err()`

## `caveman.runtime_identity`

Runtime identity and environment hygiene for Caveman. Solves the "environment pollution" problem: when Caveman is launched from another agent's shell (e.g., OpenClaw's nohup), it inherits environment variables that can m

**Functions:** `sanitize_environment()`, `build_clean_env()`, `get_runtime_identity()`

## `caveman.safe_stdio`

Safe stdio wrapper — prevents encoding errors from crashing the agent. Ported from Hermes _SafeWriter (MIT, Nous Research). When running under nohup, launchd, or systemd, stdout/stderr may be redirected to files with res

**Functions:** `install_safe_stdio()`

## `caveman.sandbox.__init__`

Docker sandbox — isolated code execution in containers. Provides secure execution environment with: - Network isolation (configurable) - Filesystem isolation (workspace mounts) - Resource limits (CPU, memory, timeout) - 

**Classes:** `SandboxConfig`

**Functions:** `is_docker_available()`, `run_in_docker()`, `run_sandboxed()`

## `caveman.security.__init__`

Security — permissions, secret scanning, sandbox execution, and encryption.

## `caveman.security.content_sanitizer`

External content sanitization — clean untrusted input. Prevents prompt injection and other attacks from external content (web pages, user uploads, API responses, etc.).

**Functions:** `detect_prompt_injection()`, `sanitize_external_content()`

## `caveman.security.encryption`

E2E encryption — AES-256-GCM for memory and trajectory data. Provides transparent encrypt/decrypt for sensitive local data. Key derivation via PBKDF2 from user passphrase. No external dependencies — uses stdlib `hashlib`

**Classes:** `EncryptedBlob`, `Encryptor`

## `caveman.security.osv_check`

OSV malware check for MCP extension packages. Before launching an MCP server via npx/uvx, queries the OSV API to check if the package has known malware advisories (MAL-* IDs). Regular CVEs are ignored — only confirmed ma

**Functions:** `check_package_for_malware()`

## `caveman.security.path_safety`

Path safety — prevent path traversal and symlink attacks. Validates file paths before operations to prevent: - Directory traversal (../../etc/passwd) - Symlink following to sensitive locations - Access to system director

**Functions:** `is_safe_path()`, `resolve_safe_path()`

## `caveman.security.permissions`

Permission tiering — controls agent action permissions.

**Classes:** `PermissionLevel`, `PermissionManager`

## `caveman.security.redact`

Regex-based secret redaction for logs and tool output. Ported from Hermes redact.py (MIT, Nous Research). Masks API keys, tokens, credentials, phone numbers, private keys, and database connection strings before they reac

**Classes:** `RedactingFormatter`

**Functions:** `redact_text()`

## `caveman.security.sandbox`

Sandbox — isolated code execution environment. Security model (honest about limitations): - Process isolation via subprocess (NOT container/chroot/seccomp) - Path containment for preloaded files (validated) - Process gro

**Classes:** `SandboxResult`, `SandboxConfig`, `Sandbox`

## `caveman.security.scanner`

Secret scanner — prevent credential leakage. Design principles (long-term / highest compound interest): 1. Every pattern has a test vector — if regex breaks, test catches it 2. Patterns are data, not code — easy to add/a

**Classes:** `SecretPattern`, `ScanResult`

**Functions:** `scan()`, `redact()`, `assert_clean()`

## `caveman.security.skill_guard`

Skill Guard — security scanner for externally-sourced skills. Ported from Hermes skills_guard.py (MIT, Nous Research) with Caveman adaptations. Scans skill files for dangerous patterns before installation.

**Classes:** `TrustLevel`, `Finding`, `ScanResult`

**Functions:** `scan_skill()`, `should_allow_install()`, `format_scan_report()`

## `caveman.security.website_policy`

Website access policy for URL-capable tools. Loads a user-managed website blocklist from config and optional shared list files. Browser/web tools call check_url() before accessing any URL. Policy is cached in memory with

**Functions:** `check_url()`, `clear_cache()`

## `caveman.skills.__init__`

Skills subsystem — reusable agent capabilities with RL routing.

## `caveman.skills.executor`

Skill executor — run skills with quality gate enforcement.

**Classes:** `StepResult`, `SkillResult`, `SkillExecutor`

## `caveman.skills.harness`

Skill Harness — Guide + Sensor abstraction for skill execution. Guide: Pre-execution instructions injected into the agent's context. - System prompt additions - Tool restrictions - Output format requirements Sensor: Post

**Classes:** `GuideConfig`, `SensorCheck`, `SensorResult`, `Sensor`, `SkillHarness`

## `caveman.skills.manager`

Skill manager v2 — load, save, match, auto-create, evolve.

**Classes:** `SkillManager`

## `caveman.skills.rl_router`

Skill RL Router — reinforcement learning-based skill selection. Replaces semantic similarity matching with a learned router that improves over time based on success/failure feedback. Phase 1 (this file): Multi-armed band

**Classes:** `ArmStats`, `SkillRLRouter`

## `caveman.skills.sync`

Skills sync — manifest-based seeding and updating of bundled skills. Copies bundled skills from the repo's skills/ directory into ~/.caveman/skills/ and uses a manifest to track which skills have been synced and their or

**Functions:** `sync_skills()`

## `caveman.skills.types`

Skill definition types.

**Classes:** `SkillTrigger`, `SkillStep`, `QualityGate`, `Skill`

## `caveman.skills.utils`

Skill utilities — frontmatter parsing, platform filtering, discovery. Ported from Hermes skill_utils.py (MIT, Nous Research). Adapted for Caveman's skill system.

**Functions:** `parse_frontmatter()`, `skill_matches_platform()`, `extract_skill_description()`, `extract_conditions()`, `iter_skill_files()`, `discover_skills()`

## `caveman.timeouts`

Centralized timeout constants. All hardcoded timeouts should reference these constants instead of magic numbers. Grouped by semantic category for easy tuning. To override at runtime, set the corresponding environment var

## `caveman.tools.__init__`

Tool system — registry and built-in tools.

## `caveman.tools.budget_config`

Budget configuration for tool result persistence. Controls when tool results are persisted to disk vs kept inline. Three layers: - Per-result: threshold in chars (tool-specific overrides) - Per-turn: aggregate char budge

**Classes:** `BudgetConfig`

## `caveman.tools.builtin.__init__`

Built-in tools — bash, browser, coding agent, file ops, web search, memory, process, delegate.

## `caveman.tools.builtin.acp_tool`

ACP tools — send tasks to remote ACP agents.

**Functions:** `acp_send()`, `acp_status()`, `acp_cancel()`

## `caveman.tools.builtin.approval`

Command Approval — dangerous command detection and approval flow. Detects dangerous shell commands and manages approval state per session. Extracted from Hermes tools/approval.py (923 lines).

**Classes:** `DangerousCommandResult`, `ApprovalEntry`, `ApprovalManager`

**Functions:** `set_current_session_key()`, `get_current_session_key()`, `detect_dangerous_command()`

## `caveman.tools.builtin.bash`

Bash tool — execute shell commands with safety, timeout, and output management. Ported safety patterns from Hermes shell execution (MIT, Nous Research). Safety layers: 1. Dangerous pattern blocking (rm -rf /, fork bomb, 

**Functions:** `register_gateway_pid()`, `bash_exec()`

## `caveman.tools.builtin.branch_tool`

Conversation branch tool — fork the current conversation to explore alternatives. Allows the agent to create a snapshot of the current context, explore a different approach, and optionally merge back.

**Functions:** `conversation_branch()`

## `caveman.tools.builtin.browser`

Browser automation tool — @tool-decorated, lifecycle-aware. Two backends: 1. OpenClaw bridge: Use OpenClaw's browser tool via MCP 2. Direct Playwright via BrowserManager: Multi-tab, SSRF-safe, cookies Actions: navigate, 

**Functions:** `set_bridge()`, `close_browser()`, `browser_dispatch()`

## `caveman.tools.builtin.browser_depth`

Browser Depth — vision analysis, recording, screenshot cleanup, camofox. Supplements browser_v2.py with vision AI analysis, session recording, and anti-fingerprint browser support. Extracted from Hermes browser_tool.py (

**Classes:** `VisionConfig`, `RecordingSession`, `CamofoxProfile`

**Functions:** `browser_vision()`, `save_screenshot()`, `cleanup_old_screenshots()`, `cleanup_old_recordings()`, `get_camofox_profile()`

## `caveman.tools.builtin.browser_manager`

Browser Manager — multi-tab, cookie, SSRF protection, Canvas support. Extends the basic browser tool with production-grade features: - Multi-tab management (open/close/focus/list) - Cookie persistence - SSRF protection (

**Classes:** `BrowserManager`

**Functions:** `is_ssrf_safe()`

## `caveman.tools.builtin.browser_providers`

Browser Providers — cloud browser backends (BrowserBase, Browser-Use). Provides pluggable cloud browser backends for headless browsing. Extracted from Hermes browser_providers/ (608 lines).

**Classes:** `CloudBrowserProvider`, `BrowserSession`, `BrowserbaseProvider`, `BrowserUseProvider`

**Functions:** `get_provider()`, `list_providers()`

## `caveman.tools.builtin.browser_v2`

Browser Tool v2 — full browser automation with vision, console, recording. Extracted from Hermes browser_tool.py (2387 lines). Adds: vision analysis, console access, scroll, back, press, recording, session cleanup, scree

**Functions:** `browser_navigate()`, `browser_snapshot()`, `browser_click()`, `browser_type()`, `browser_scroll()`, `browser_back()`, `browser_press()`, `browser_console()`, `browser_get_images()`, `browser_close()`, `cleanup_all_browsers()`

## `caveman.tools.builtin.checkpoint_manager`

Checkpoint Manager — save and restore agent state. Provides checkpoint/restore for agent sessions, enabling recovery from crashes and session migration. Extracted from Hermes tools/checkpoint_manager.py (623 lines).

**Classes:** `Checkpoint`, `CheckpointManager`

## `caveman.tools.builtin.checkpoint_tool`

Checkpoint tools — save and restore agent conversation state.

**Functions:** `checkpoint_save()`, `checkpoint_restore()`, `checkpoint_list()`

## `caveman.tools.builtin.clarify_tool`

Clarify tool — ask the user a clarifying question before proceeding. When the agent is unsure about the user's intent, it can use this tool to ask a specific question and wait for the answer.

**Functions:** `clarify()`

## `caveman.tools.builtin.code_execution`

Code Execution — sandboxed code execution with timeout and output capture. Provides safe code execution in isolated environments with resource limits, timeout, and output capture. Extracted from Hermes tools/code_executi

**Classes:** `ExecutionConfig`, `ExecutionResult`

**Functions:** `execute_code()`

## `caveman.tools.builtin.coding_agent`

Coding agent tool — delegate coding tasks to external AI agents.

**Functions:** `coding_agent_tool()`

## `caveman.tools.builtin.cron_tool`

Cron tool — manage scheduled tasks.

**Functions:** `cron_tool()`

## `caveman.tools.builtin.cronjob`

Cronjob Tools — scheduled task management. Provides cron-like scheduling for recurring agent tasks. Extracted from Hermes tools/cronjob_tools.py (542 lines).

**Classes:** `CronJob`, `CronManager`

**Functions:** `parse_interval()`

## `caveman.tools.builtin.delegate_tool`

Delegate Tool — sub-agent delegation with concurrency and timeout. Extracted from Hermes delegate_tool.py (1103 lines). Key patterns: parallel delegation, result merging, timeout, cancellation.

**Classes:** `DelegateTask`, `DelegateManager`

**Functions:** `get_delegate_manager()`, `set_delegate_agent_fn()`, `delegate_task()`, `delegate_parallel()`

## `caveman.tools.builtin.file_ops`

File operation tools — read, write, edit, search, list. Production-grade file operations with line numbers, binary detection, search (grep-like), and write-denied path protection. Ported patterns from Hermes file_operati

**Functions:** `file_read()`, `file_write()`, `file_edit()`, `file_search()`, `file_list()`

## `caveman.tools.builtin.file_ops_v2`

File Operations v2 — read with pagination, unified diff. Extracted from Hermes file_operations.py (1152 lines). Key patterns: patch/replace with context, search with ripgrep, glob, binary detection.

**Functions:** `is_binary()`, `add_line_numbers()`, `file_read_v2()`, `file_diff()`

## `caveman.tools.builtin.file_tools`

File tools — full-depth port from Hermes file_tools.py. Features ported: - Device path blocklist (infinite output / blocking input) - Sensitive path protection (/etc/, /boot/, docker.sock) - Read dedup (skip re-reads of 

**Functions:** `read_file()`, `replace_in_file()`, `create_file()`, `write_file()`, `patch_file()`

## `caveman.tools.builtin.file_tools_search`

File tools — search and diff operations. Extracted from file_tools.py to keep modules under 450 lines.

**Functions:** `search_in_file()`, `file_diff()`

## `caveman.tools.builtin.file_tools_tracker`

File tools tracker — guards, read tracking, dedup, staleness detection. Extracted from file_tools.py to keep modules under 450 lines.

**Functions:** `is_binary_file()`, `is_blocked_device()`, `check_sensitive_path()`, `get_read_files_summary()`, `clear_read_tracker()`, `reset_file_dedup()`, `notify_other_tool_call()`

## `caveman.tools.builtin.flywheel_tool`

Flywheel tool — trigger self-audit from within agent conversations. Allows Caveman to run its own flywheel (audit → fix → test → commit) when asked via Discord or any other gateway.

**Functions:** `flywheel_exec()`

## `caveman.tools.builtin.gateway_tool`

Gateway tools — send messages and list gateways.

**Functions:** `gateway_send()`, `gateway_list()`

## `caveman.tools.builtin.homeassistant_tool`

Home Assistant integration — control smart home devices via REST API. Tools: - ha_list_entities: List/filter entities by domain or area - ha_get_state: Get detailed state of a single entity - ha_call_service: Call a HA s

**Functions:** `ha_list_entities()`, `ha_get_state()`, `ha_call_service()`

## `caveman.tools.builtin.image_gen_tool`

Image generation tool — generate/edit images via DALL-E or Stability AI.

**Functions:** `image_generate()`, `image_edit()`

## `caveman.tools.builtin.image_generation`

Image Generation — text-to-image via multiple providers. Provides image generation via DALL-E, Stable Diffusion, and other providers. Extracted from Hermes tools/image_generation_tool.py (703 lines).

**Classes:** `ImageGenConfig`, `GeneratedImage`, `ImageGenResult`

**Functions:** `generate_image()`, `check_image_gen_available()`

## `caveman.tools.builtin.llm_router`

LLM Router — unified interface for calling different LLM providers. Provides a single call_llm() function that routes to the appropriate provider based on model name. Extracted from Hermes LLM routing.

**Classes:** `LLMResponse`, `LLMConfig`

**Functions:** `resolve_provider()`, `call_llm()`

## `caveman.tools.builtin.mcp_client`

MCP Client — Model Context Protocol integration. Extracted from Hermes mcp_tool.py (2195 lines). Key patterns: stdio/SSE transport, tool discovery, sampling handler.

**Classes:** `MCPTool`, `MCPServer`, `MCPClient`

## `caveman.tools.builtin.mcp_lifecycle`

MCP Server Lifecycle — health checks, auto-reconnect, env safety. Extracted from Hermes mcp_tool.py MCPServerTask (480 lines).

**Classes:** `ServerHealth`, `MCPServerConfig`, `MCPServerLifecycle`

**Functions:** `build_safe_env()`

## `caveman.tools.builtin.mcp_sampling`

MCP Sampling Handler — handles sampling/createMessage requests. Extracted from Hermes mcp_tool.py SamplingHandler (350 lines). Provides LLM-backed sampling for MCP servers that request it.

**Classes:** `SamplingConfig`, `SamplingMetrics`, `SamplingHandler`

## `caveman.tools.builtin.mcp_tool`

MCP tools — connect to external MCP servers and use their tools.

**Functions:** `mcp_connect()`, `mcp_list_tools()`, `mcp_call()`, `mcp_disconnect()`

## `caveman.tools.builtin.media_understanding`

Media Understanding — image, audio, and document analysis. Provides media analysis capabilities for understanding attachments in messages. Core patterns from OpenClaw src/media-understanding/ (10K LOC).

**Classes:** `MediaAnalysis`

**Functions:** `analyze_image()`, `analyze_document()`, `detect_media_type()`

## `caveman.tools.builtin.memory_tool`

Memory tools — search, store, and retrieve agent memories.

**Functions:** `memory_search()`, `memory_store()`, `memory_recent()`

## `caveman.tools.builtin.metrics_tool`

Metrics tool — query agent performance and flywheel stats. Lets Caveman (and users) inspect token usage, timing percentiles, flywheel history, and session costs. Essential for self-optimization.

**Functions:** `metrics_query()`

## `caveman.tools.builtin.mixture_of_agents`

Mixture-of-Agents (MoA) — multi-model collaboration for complex reasoning. Full port from Hermes: parallel reference models → aggregator synthesis, retry + backoff, debug session tracking, reasoning extraction.

**Classes:** `MoAResponse`, `MoAResult`

**Functions:** `mixture_of_agents()`, `check_moa_requirements()`, `get_debug_session_info()`, `get_available_models()`, `get_moa_configuration()`

## `caveman.tools.builtin.moa_tool`

Mixture-of-Agents — multi-model collaborative reasoning. Based on "Mixture-of-Agents Enhances Large Language Model Capabilities" (Wang et al., arXiv:2406.04692v1). Architecture: 1. Reference models generate diverse respo

**Functions:** `mixture_of_agents()`, `mixture_of_agents_tool()`

## `caveman.tools.builtin.process_registry`

Process Registry — background process management with PTY support. Extracted from Hermes process_registry.py (1172 lines). Key patterns: spawn, poll, read log, write stdin, kill, session tracking.

**Classes:** `ProcessSession`, `ProcessRegistry`

## `caveman.tools.builtin.process_tool`

Process tools — start, list, read output, kill background processes.

**Functions:** `process_start()`, `process_list()`, `process_output()`, `process_kill()`

## `caveman.tools.builtin.progress_tool`

Progress tool — send status updates to the current conversation channel. Simpler than gateway_send: auto-detects the source channel from context. Also tracks whether progress was sent, so gateway can skip duplicate final

**Functions:** `progress_send()`

## `caveman.tools.builtin.sandbox_tool`

Sandbox tool — safe Python code execution in isolated subprocess.

**Functions:** `sandbox_exec()`, `sandbox_eval()`

## `caveman.tools.builtin.send_message_tool`

Send Message Tool — cross-platform message sending. Extracted from Hermes send_message_tool.py (1049 lines). Enables the agent to send messages to any connected platform.

**Functions:** `register_platform()`, `get_platform()`, `list_platforms()`, `send_message()`, `send_typing_indicator()`

## `caveman.tools.builtin.session_search_tool`

Session search tool — search across conversation history. Enables the agent to search its own past conversations for context, decisions, and patterns. Uses FTS5 for fast full-text search.

**Functions:** `session_search()`

## `caveman.tools.builtin.skill_manager_tool`

Skill manager tool — create, list, show, edit, delete learned skills. Integrates with Skill Guard for security scanning on create/edit.

**Functions:** `skill_create()`, `skill_edit()`, `skill_list()`, `skill_show()`, `skill_delete()`

## `caveman.tools.builtin.skills_guard`

Skills Guard — security validation for skill bundles. Validates skill bundles before installation, checking for dangerous patterns, excessive permissions, and policy violations. Extracted from Hermes tools/skills_guard.p

**Classes:** `ThreatPattern`, `ScanResult`

**Functions:** `scan_bundle()`, `quarantine_skill()`, `list_quarantined()`, `log_audit()`, `guard_install()`

## `caveman.tools.builtin.skills_hub`

Skills Hub — skill discovery, installation, and management. Extracted from Hermes skills_hub.py (2775 lines). Key patterns: GitHub source, local source, search, install, quarantine, audit.

**Classes:** `SkillMeta`, `SkillBundle`, `SkillSource`, `LocalSource`, `GitHubSource`, `HubLockFile`

**Functions:** `quarantine_bundle()`, `install_from_quarantine()`, `install_bundle()`, `uninstall_skill()`, `create_source_router()`, `unified_search()`

## `caveman.tools.builtin.skills_hub_depth`

Skills Hub Depth — GitHub tree API, validation, update checking, parallel search. Supplements skills_hub.py with deeper GitHub integration and safety features. Extracted from Hermes skills_hub.py (2775 lines) — the parts

**Classes:** `GitHubAuth`

**Functions:** `validate_skill_name()`, `validate_bundle_path()`, `download_directory_via_tree()`, `find_skill_in_repo()`, `parse_frontmatter()`, `check_for_updates()`, `parallel_search()`, `audit_bundle_security()`

## `caveman.tools.builtin.skills_sync`

Skills sync — discover, checksum, diff, and sync skills from remote/bundled. Full port from Hermes tools/skills_sync.py: - Bundled skills discovery (shipped with the package) - Manifest read/write with checksums - Direct

**Classes:** `SkillManifest`, `SyncState`, `SyncDiff`

**Functions:** `compute_skill_checksum()`, `scan_local_skills()`, `load_sync_state()`, `save_sync_state()`, `diff_skills()`, `sync_skill_from_remote()`, `sync_all_bundled()`, `get_sync_status()`

## `caveman.tools.builtin.terminal_tool`

Terminal tool — unified command execution across environments. Extends the basic bash tool with: - Environment selection (local/docker/ssh) via config - Background task management - Automatic cleanup after inactivity - U

**Classes:** `BackgroundTask`, `TaskRegistry`

**Functions:** `execute_command()`, `terminal_tool()`

## `caveman.tools.builtin.terminal_v2`

Terminal Tool v2 — compatibility shim. All functionality has been merged into terminal_tool.py. This module re-exports symbols for backward compatibility.

**Functions:** `terminal_execute()`

## `caveman.tools.builtin.todo_tool`

Todo tool — persistent task list stored as JSON.

**Functions:** `todo_add()`, `todo_list()`, `todo_done()`, `todo_remove()`

## `caveman.tools.builtin.tool_result_storage`

Tool Result Storage — persist and retrieve tool execution results. Stores tool results for later reference, deduplication, and caching. Extracted from Hermes tools/tool_result_storage.py.

**Classes:** `StoredResult`, `ToolResultStore`

## `caveman.tools.builtin.tool_wrappers`

Tool wrappers — @tool-decorated bridge functions for auto-discovery. Each wrapper provides: - Input validation (type checks, required fields) - Error handling with structured error responses - Proper JSON serialization o

**Functions:** `file_replace()`, `file_create()`, `file_patch()`, `analyze_image_tool()`, `analyze_document_tool()`, `moa_tool()`, `transcribe_audio_tool()`, `generate_image_tool()`, `checkpoint_create_tool()`, `checkpoint_restore_tool()`, `security_audit_tool()`, `skills_sync_status_tool()`

## `caveman.tools.builtin.transcribe_tool`

Transcription tool — audio/video to text via whisper CLI.

**Functions:** `transcribe()`, `transcribe_url()`

## `caveman.tools.builtin.transcription`

Transcription Tools — audio/video to text conversion. Provides transcription via Whisper API and local fallbacks. Extracted from Hermes tools/transcription_tools.py (708 lines).

**Classes:** `TranscriptionConfig`, `TranscriptionResult`

**Functions:** `transcribe()`, `check_transcription_available()`

## `caveman.tools.builtin.tts_depth`

TTS Depth — multi-provider, Opus conversion, streaming, markdown strip. Supplements tts_v2.py with ElevenLabs, OpenAI, MiniMax, Mistral, NeuTTS providers, Opus conversion, and streaming TTS. Extracted from Hermes tts_too

**Classes:** `TTSProviderConfig`

**Functions:** `load_tts_config()`, `strip_markdown_for_tts()`, `has_ffmpeg()`, `convert_to_opus()`, `generate_elevenlabs()`, `generate_openai_tts()`, `generate_minimax_tts()`, `generate_edge_tts()`, `text_to_speech()`

## `caveman.tools.builtin.tts_tool`

Text-to-Speech — multi-provider TTS with format conversion. Supported providers: - Edge TTS (default, free): Microsoft Edge neural voices - OpenAI TTS: High quality, needs OPENAI_API_KEY - ElevenLabs: Premium voices, nee

**Classes:** `TTSProvider`

**Functions:** `synthesize()`, `list_edge_voices()`, `text_to_speech_tool()`

## `caveman.tools.builtin.tts_v2`

TTS Tool v2 — text-to-speech with multiple providers. Extracted from Hermes tts_tool.py (1059 lines). Supports: system TTS, OpenAI TTS, ElevenLabs, edge-tts.

**Classes:** `TTSConfig`

**Functions:** `register_tts_provider()`, `tts_generate()`, `check_tts_requirements()`

## `caveman.tools.builtin.url_safety_tool`

URL safety tool — check URLs for suspicious patterns.

**Functions:** `url_check()`

## `caveman.tools.builtin.vision_tool`

Vision tool — prepare image payloads for LLM vision processing.

**Functions:** `vision_describe()`

## `caveman.tools.builtin.vision_tools`

Vision Tools — image analysis and processing utilities. Provides vision-related utilities for screenshot analysis, image resizing, and multi-modal content handling. Extracted from Hermes tools/vision_tools.py.

**Functions:** `is_image_size_error()`, `resize_image_for_vision()`, `image_to_data_url()`, `build_vision_message()`, `extract_image_urls()`

## `caveman.tools.builtin.voice_mode`

Voice mode — push-to-talk audio recording and STT/TTS pipeline. Provides: - Audio capture via sounddevice (optional dependency) - WAV encoding via stdlib wave - STT dispatch via transcription tools - TTS playback via sys

**Classes:** `VoiceRecorder`, `AudioPlayer`, `VoiceMode`

## `caveman.tools.builtin.web_fetch_depth`

Web Fetch Depth — readability, PDF, robots.txt, caching, crawl, multi-backend. Supplements web_fetch_v2.py with deeper web extraction capabilities. Extracted from Hermes web_tools.py (2103 lines).

**Classes:** `SearchBackend`

**Functions:** `get_available_backend()`, `check_url_secrets()`, `check_robots_txt()`, `clean_base64_images()`, `extract_readable_content()`, `cache_key()`, `get_cached()`, `set_cached()`, `cleanup_cache()`, `crawl_site()`, `parallel_extract()`

## `caveman.tools.builtin.web_fetch_v2`

Web Tool v2 — comprehensive web interaction with multiple backends. Extracted from Hermes web_tool.py (2100 lines). Supports: Firecrawl, Jina Reader, raw HTTP, readability extraction.

**Classes:** `WebResult`

**Functions:** `web_fetch()`

## `caveman.tools.builtin.web_research`

Web Research Environment — multi-step web research with scoring. Provides a structured environment for multi-step web research tasks with reward signals for correctness, diversity, and efficiency. Extracted from Hermes e

**Classes:** `ResearchStep`, `ResearchScore`, `ResearchSession`, `WebResearchRunner`

## `caveman.tools.builtin.web_search`

Web search tool — Tavily API integration.

**Functions:** `web_search()`

## `caveman.tools.builtin.web_tools_v2`

Web Tools v2 — extraction, caching, SSRF protection, proxy, retry. Extracted from Hermes web_tools.py (2103 lines). Key patterns: multi-backend extraction, content summarization, SSRF guard.

**Functions:** `web_fetch_v2()`, `web_search_v2()`

## `caveman.tools.builtin.website_policy`

Website Policy — robots.txt, rate limiting, domain rules. Manages per-domain access policies for web tools. Extracted from Hermes tools/website_policy.py (282 lines).

**Classes:** `DomainPolicy`, `WebsitePolicyManager`

## `caveman.tools.credential_files`

Credential file registry for remote environments. Remote backends (Docker, SSH) need host-side credential files mounted. This module tracks which files to mount, using ContextVar for session isolation.

**Functions:** `register_credential_file()`, `get_credential_file_mounts()`, `get_skills_directory_mount()`, `get_cache_directory_mounts()`, `clear_registered()`

## `caveman.tools.debug_helpers`

Debug session infrastructure for tools. Provides a lightweight debug logger that records tool calls to a JSON log file. Activated by a tool-specific environment variable. When disabled, all methods are cheap no-ops.

**Classes:** `DebugSession`

## `caveman.tools.display`

Tool display metadata — emojis and labels for tool calls. Used by heartbeat, progress indicators, and status displays.

**Functions:** `tool_display()`, `format_tool_call()`

## `caveman.tools.env_passthrough`

Environment variable passthrough registry for sandboxed execution. Skills that declare required_environment_variables need those vars available in sandboxed environments. This module provides a session-scoped allowlist s

**Functions:** `register_passthrough()`, `is_passthrough()`, `get_passthrough_env()`, `clear_passthrough()`

## `caveman.tools.fuzzy_match`

Fuzzy matching for file edit operations. 8-strategy matching chain to robustly find and replace text, accommodating LLM-generated code variations in whitespace, indentation, escaping. Strategies (tried in order): 1. Exac

**Functions:** `fuzzy_find_and_replace()`

## `caveman.tools.interrupt`

Per-thread interrupt signaling for concurrent tool execution. Thread-scoped so interrupting one gateway session doesn't kill tools in other sessions running in the same process.

**Functions:** `set_interrupt()`, `is_interrupted()`, `clear_all()`

## `caveman.tools.patch_parser`

V4A Patch Format Parser. Parses the V4A patch format used by Codex, Cline, and other coding agents. V4A Format: *** Begin Patch *** Update File: path/to/file.py @@ optional context hint @@ context line (space prefix) -re

**Classes:** `PatchOp`, `PatchOperation`

**Functions:** `parse_v4a_patch()`, `apply_v4a_operations()`

## `caveman.tools.registry`

Tool registry v2 — declarative self-registering tools. Instead of 54 lines of manual registration, tools declare their own schema via the @tool decorator. The registry auto-discovers them. Usage: from caveman.tools.regis

**Classes:** `ToolRegistry`

**Functions:** `tool()`

## `caveman.tools.result_storage`

Tool result persistence — preserves large outputs instead of truncating. Inspired by Hermes tool_result_storage.py (MIT, Nous Research). Three-layer defense against context overflow: 1. Per-tool output cap (inside each t

**Functions:** `persist_tool_result()`, `enforce_turn_budget()`, `cleanup_old_results()`

## `caveman.training.__init__`

Training & data pipeline — embedding fine-tuning + data export for researchers.

## `caveman.training.backfill_trajectories`

Backfill trajectory metadata — recount tool_calls from conversation data. Fixes trajectories saved before the tools_exec.py recording fix (commit 69f24d3). Those trajectories have tool_calls=0 because the recorder was ch

**Classes:** `BackfillStats`

**Functions:** `backfill_tool_calls()`

## `caveman.training.batch_runner`

Batch runner — run multiple tasks in parallel for training data generation. Ported from Hermes batch_runner.py (MIT, Nous Research), simplified for Caveman. Used by the training pipeline to generate trajectories at scale

**Classes:** `BatchConfig`, `TaskResult`, `BatchResult`, `BatchRunner`

## `caveman.training.cli_handler`

CLI handler for `caveman train` — dispatches to embedding/sft/rl targets.

**Functions:** `run_train()`

## `caveman.training.contrastive_pairs`

Contrastive trajectory pair generator for DPO/SFT training. Generates (chosen, rejected) pairs from trajectory data by: 1. Scoring all trajectories with quality heuristics 2. Pairing high-quality (chosen) with low-qualit

**Classes:** `ContrastivePairStats`

**Functions:** `generate_contrastive_pairs()`

## `caveman.training.embedding`

Embedding model training — fine-tune local embedding for better memory retrieval. Core training target for Caveman: make memory search understand user context. Example: user says "that server" → embedding maps to the cor

**Classes:** `EmbeddingTrainConfig`, `QueryMemoryPair`, `PairExtractor`, `EmbeddingTrainer`

## `caveman.training.eval_embedding`

Embedding evaluation — measure retrieval quality before/after training. Metrics: - Recall@K: what fraction of relevant memories appear in top-K results - MRR (Mean Reciprocal Rank): average 1/rank of first relevant resul

**Classes:** `EvalResult`, `EmbeddingEvaluator`

## `caveman.training.flywheel_dashboard`

Flywheel Health Dashboard — observability for the self-improvement loop. Aggregates metrics from all flywheel subsystems into a single health report: - Memory: trust distribution, decay stats, retrieval rates - Skills: R

**Classes:** `FlywheelDashboard`

**Functions:** `generate_report()`

## `caveman.training.retrieval_log`

Retrieval log — SQLite-backed memory search log for embedding training. Every time Recall or memory_search runs, we log: - query: what the user/system searched for - results: which memories were returned (with scores) - 

**Classes:** `RetrievalEntry`, `RetrievalLog`

## `caveman.training.rl`

RL data export — generate preference pairs for researcher fine-tuning. Primary use: export preference data for researchers doing RLHF/DPO experiments. Caveman's own skill routing uses Thompson Sampling (rl_router.py), no

**Classes:** `RLConfig`, `PreferencePairBuilder`, `RLTrainer`

## `caveman.training.sft`

SFT data export — convert trajectories to standard training formats. Primary use: export data for researchers who want to fine-tune their own models. Caveman's own "learning" happens through memory + skills, not model tr

**Classes:** `TrainingConfig`, `DatasetBuilder`, `SFTTrainer`

## `caveman.training.stats`

Training stats — trajectory readiness analysis.

**Functions:** `show_training_stats()`

## `caveman.trajectory.__init__`

Trajectory subsystem — recording and export for training.

## `caveman.trajectory.recorder`

Trajectory recorder v2 — record, score, export agent interactions. Records in ShareGPT format for fine-tuning compatibility. Supports quality scoring, filtering, and batch export.

**Classes:** `TrajectoryRecorder`

## `caveman.utils`

Shared utilities — DRY primitives used across the codebase. Every function here exists because it was duplicated 2+ times. Adding here = commitment to never duplicate again.

**Functions:** `estimate_tokens()`, `estimate_tokens_for_model()`, `retry_async()`, `strip_code_fences()`, `parse_json_from_llm()`, `split_message()`, `detect_success()`, `detect_outcome()`

## `caveman.wiki.__init__`

Wiki Compiler — compile, don't retrieve. Inspired by Karpathy's LLM Wiki pattern (April 3, 2026): Instead of searching memories on every query, periodically compile all knowledge into a structured wiki. The wiki is a per

**Classes:** `WikiEntry`, `CompilationResult`, `WikiStore`

## `caveman.wiki.auto_trigger`

Wiki auto-trigger — compile wiki when enough new memories accumulate. Listens to NUDGE_EXTRACT events and triggers wiki compilation when the accumulation threshold is reached. This closes the knowledge crystallization lo

**Classes:** `WikiAutoTrigger`

## `caveman.wiki.compiler`

Wiki Compiler Engine — the core compilation loop. Compiles raw memories into structured wiki entries, promotes knowledge up the tier pyramid, expires stale entries, and generates the compiled context for system prompt in

**Classes:** `WikiCompiler`

## `caveman.wiki.provenance`

Wiki Provenance — source tracking and contradiction detection. Tracks where each wiki entry came from (session, tool, user input) and detects contradictions between entries in the same tier or across tiers. Contradiction

**Classes:** `Provenance`, `Contradiction`, `ProvenanceTracker`, `ContradictionDetector`

## `caveman.wiring`

Module wiring — connects all remaining orphan subsystems. Called during application startup to ensure all modules are reachable. Each import is wrapped in try/except so failures are non-fatal.

**Functions:** `wire_providers()`, `wire_memory()`, `wire_security()`, `wire_skills()`, `wire_bridges()`, `wire_coordinator()`, `wire_misc()`, `wire_agent_extras()`, `wire_all()`
